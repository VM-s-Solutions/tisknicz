# Review — T-0172 (branch feat/T-0172-checkout-flow-usability)

**Process note:** reviewer agent unavailable (account session limit) — self-review by the
implementing session against [checklist](../checklist.md) + CLAUDE.md Part 4; flagged for an
extra-skeptical glance at PR time (author == reviewer).

**Verdict: approve** (manual-browser gap + untested-but-reviewed items listed in the test plan).

## Dimensions checked
- **Scope vs ticket:** all eight scope bullets shipped; the T-0168-owned unconfirmed-email notice
  block left untouched as scoped.
- **Correctness details:** focus pass runs in an effect after the errored render commits (the
  T-0174 lesson — focusing mid-submit no-ops); confirmation-page branch ORDER is the fix (backend
  state beats the gateway redirect param), with the reasoning in a comment; the poller's final
  poll uses a functional setView so a success landing during the last poll wins over capReached.
- **State-machine honesty:** no client-side state invention — the confirmation page still renders
  only what the backend state says.
- **Rules:** no `any`; i18n via keys (one `// T-0172` block + reuse); `FileDownloadButton` reuse
  instead of a new pattern; `beforeunload` guard disarms while a submit is in flight so the
  post-create navigation is never blocked.
- **Tests:** 3 behavior tests on the highest-value form; untestable-in-jsdom items listed
  explicitly in the test plan instead of being claimed.
