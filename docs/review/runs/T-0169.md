# Review — T-0169 (branch feat/T-0169-redirect-continuity)

**Process note:** reviewer agent unavailable (session limit) — self-review against
[checklist](../checklist.md) + CLAUDE.md Part 4; flagged (author == reviewer).

**Verdict: approve**, with the admin already-signed-in panel deferred (recorded in the test plan).

## Dimensions checked
- **Security first:** every change composes redirects through the existing `safeRedirectTarget`
  guard; the new helpers only *build* hrefs and are covered by a regression test asserting
  absolute/protocol-relative targets are still rejected.
- **The helpers already existed** (`continueHref`, `audienceHome`) — this ticket is mostly wiring
  them in, plus one genuinely missing bridge (`hostToAudience`). No parallel mechanism invented.
- **A real bug surfaced during testing:** `useSearchParams()` can be null outside a router context.
  Fixed in production code, not by patching the mock — patching the test would have shipped a
  crash-on-mount path.
- **Rules:** no `any`, i18n keys for the new admin hint, no dead code, guard semantics unchanged.
