# Review — T-0166 (commit cb2b7fe, branch feat/T-0166-email-link-paths)

**Verdict: REQUEST CHANGES** — one MEDIUM item (first-occurrence guard gap). Everything else passes.

## Findings

### MEDIUM — guard gap: nothing asserts /verify, /magic, /reset resolve to real pages (blocking)
- Defect location: `frontend/src/__tests__/next-config-auth-redirects.test.ts` (pins strings only) +
  `backend/src/Makables.Tests/AppServices/Common/PublicAppUrlsOptionsValidatorTests.cs:29-37` (pins strings only).
- Missing guard: no test ties the pinned paths to the route tree. `route-group-link-hygiene.test.ts:37`
  scans `<Link>`/push/redirect *call sites* for `/auth/` prefixes — it never checks target existence.
- Trigger: rename `frontend/src/app/(auth)/verify/` (the exact refactor the ticket warns against,
  "do not 'fix' by renaming the frontend route") → all email links 404 again, every test stays green.
- Guard-check performed: backend pin tests, redirect-config test, hygiene test, verify-client tests —
  all traced; none fails on a leaf rename (client tests move with the directory).
- Required fix (cheap, same PR per checklist/charter first-occurrence rule — this bug class shipped
  twice as `<Link>`s before hygiene landed): assert `src/app/(auth)/{verify,magic,reset}/page.tsx`
  exist, e.g. ~6 lines of `fs.existsSync` in `next-config-auth-redirects.test.ts`.

### LOW — nine test fixtures still hardcode the dead `/auth/*` templates (non-blocking)
- e.g. `ShipOrderHandlerTests.cs:79-81`, `MarkOrderPaidHandlerTests.cs:90-92`, `AcceptOrderHandlerTests.cs:69-71`,
  `HandOverOrderHandlerTests.cs:68-70`, `CancelExpiredOrderHandlerTests.cs:50-52`, `MarkOrderDeliveredHandlerTests.cs:58-60`,
  both `Post*OrderMessageHandlerTests`, `RefundOrderHandlerTests`. Verified benign: those tests assert only
  `WebBaseUrl`-composed order URLs; the three templates are never consumed. But they perpetuate dead paths
  as copy-paste source — drop the three properties (use defaults), trivially foldable into the fix commit.

## Dimensions checked (one line each)

1. **Correctness** — pass. `(auth)` group adds no segment; real pages `verify/`, `magic/`, `reset/` all
   consume `searchParams.get('token')`; `/auth/confirm → /verify` leaf rename covered in defaults,
   redirects, and both test layers; `BuildActionUrl` composition contract mirrored faithfully in AC-1 test.
2. **Completeness** — pass. Grep sweep: no other backend/frontend composer builds frontend `/auth/*` URLs
   (backend `/auth/*` hits are API-host OAuth callbacks/endpoints, out of scope); appsettings*.json and
   `infra/bicep` (`app-service.bicep:80`, `main.bicep:82`) override only `WebBaseUrl` — no path override
   defeats the fix.
3. **Tests** — pass (verified locally: backend filtered run 43/43; vitest redirect+hygiene 3/3). Pin tests
   fail on any regression to `/auth/*`; `EmailSendServiceTests` fixture now exercises code defaults (`new()`
   keeps initializers), closing the masking the old explicit fixture caused. Same-commit tests = Gate 5
   "alongside" — OK. AC-2 query preservation is framework behavior, covered by manual curl (test plan row 7).
4. **next.config.ts** — pass. Correct async `redirects()` shape; `permanent: true` → 308; destinations carry
   no query so incoming `?token` auto-forwards; config redirects run before rewrites and the proxy sources
   (`/api-proxy/:host/*`) are disjoint — no conflict; hygiene test scans only `src/`, no false positive.
5. **Project rules** — pass. No `any`/`console.*`/dead code; comments are why-comments with ticket refs;
   check-consistency: 13 findings, all pre-existing, none in touched files (verified by grep against the run).
6. **AC traceability** — AC-1 unit tests; AC-2 config test + manual 308 curl; AC-3 manual round trip with DB
   evidence (`docs/test-plans/T-0166.md`, executed 2026-08-21). All three named.

## Re-review
Only the MEDIUM item blocks. Add the page-existence assertions (and optionally the LOW fixture sweep) — no
other re-verification needed.

## Resolution (2026-08-21, post-fold)

Both findings addressed in the review-fold commit: the MEDIUM route-existence guard now lives in
`next-config-auth-redirects.test.ts` (proven to fire — an initial wrong path resolution failed the
assertion), and the LOW dead `/auth/*` fixture templates were dropped from all 8 handler test
files. Suites after fold: Makables.Tests 2079/2079, frontend vitest 173/173 (31 files).
Re-review scope was the one added test — satisfied. **Effective verdict: APPROVE.**
