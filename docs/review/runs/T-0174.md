# Review — T-0174 (branch feat/T-0174-maker-form-feedback)

**Process note:** the dedicated reviewer agent could not run (account session limit); this pass was
performed by the implementing session wearing the reviewer hat, against
[checklist](../checklist.md) + CLAUDE.md Part 4. Flagged for an extra-skeptical human/agent glance
at PR time precisely because author == reviewer.

**Verdict: approve** (with the manual-browser gap recorded in the test plan).

## Dimensions checked
- **Scope vs ticket:** all eight scope bullets implemented; set-primary/reorder correctly left to
  T-0179; decline/reactivation correctly reduced to interim copy/guards (Q-0040/Q-0041).
- **Correctness:** the one defect found during the pass — `applyError` focusing a still-disabled
  input (no-op) — was fixed (effect-based focus after re-enable) and is pinned by a test.
- **Rules:** no `any`, no `console.*`, no inline layout styles, i18n keys in one `// T-0174`
  block (+ one copy edit to the delete-confirm body), ui primitives reused (`SaveButton`, `Alert`,
  `Button`), stable keys, server components stay server (filter chips are `<Link>`s).
- **History/i18n hygiene:** display filter is URL-state (`?filter=`), round-trips through
  pagination hrefs.
- **Tests:** 12 new tests, all behavior-level (re-enable, dirty tracking, focus, queue, cap);
  no markup-snapshot tests; single-argument `expect` only (CI tsc constraint).
- **Known limitations (accepted, documented):** activity filter filters the fetched page only;
  browser pass on real maker session pending (test plan row 6).
