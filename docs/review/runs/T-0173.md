# Review — T-0173 (branch feat/T-0173-dashboard-polish)

**Process note:** reviewer agent unavailable (session limit) — self-review against
[checklist](../checklist.md) + CLAUDE.md Part 4; flagged (author == reviewer).

**Verdict: approve**, with CUST-L4 blocked on a contract change and two LOWs deferred (test plan).

## Dimensions checked
- **CUST-M1 was a rule violation, not just a UX gap:** `lib/runtime/errors.ts` says a raw backend
  message never reaches the UI, and both profile pages printed one. The new shared component makes
  that structurally hard to repeat, and its test asserts the raw string is absent.
- **CUST-L4 was checked, not guessed:** I read the generated DTOs before deferring — neither the
  list nor the detail carries `productId`/`makerSlug`, so the "link the product" fix is a contract
  change, not a frontend one. Recorded with that evidence rather than quietly skipped.
- **Retry semantics are now honest per failure type:** transient → re-run the same URL; validation
  → offer to clear the filters that caused it. Sending both to the bare route (the old behaviour)
  was wrong for the first and accidentally right for the second.
- **Payouts:** the bank-account read is `Promise.all`'d and failure-tolerant — a profile read
  failure must not break the payouts page; the banner simply doesn't render.
- **Rules:** no `any`, i18n in one `// T-0173` block, `RefreshButton` reused (no new mechanism),
  server components stay server.
