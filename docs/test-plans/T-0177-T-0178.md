# Test plan — admin read-integrity bundle (T-0177 + T-0178)

Executed 2026-08-22 on `feat/admin-read-integrity-bundle`. Findings: ADM-H1, ADM-H2, ADM-M9.
(These were the last two HIGH findings of the Phase 8 audit.)

## Automated — backend

| # | Check | Where | Result |
|---|---|---|---|
| 1 | `targetId` reaches the audit-log query filter (ADM-H2) | `AdminReadIntegrityHandlerTests` | pass |
| 2 | `targetId` length-bounded; absent stays valid (the global log is still legitimate) | same | pass |
| 3 | Lookup returns the server-resolved identity incl. in-flight count | same | pass |
| 4 | Unknown identifier → `user.notFound`, **never** "already erased" (ADM-M9) | same | pass |
| 5 | An already-erased account still **resolves** so the UI can say so | same | pass |
| 6 | Validator requires exactly one selector (neither / both rejected); malformed email rejected | same | pass |
| 7 | `Makables.Tests` full suite | **2091/2091** (+9 new) | pass |
| 8 | Lookup is admin-audience gated (anonymous → 401) | `AdminReadIntegrityIntegrationTests` | compiles; runs in CI |
| 9 | 404 writes **no** audit row; success writes exactly one `user.lookup` row targeting the **resolved id**, never the typed email (T-0137) | same | compiles; runs in CI |
| 10 | `?targetId=` accepted and applied on the audit-log endpoint | same | compiles; runs in CI |

## Automated — frontend

| # | Check | Result |
|---|---|---|
| 11 | `npx tsc --noEmit` clean; vitest **215/215** (40 files) | pass |

## Contract

| # | Check | Result |
|---|---|---|
| 12 | NSwag regen for `GET /admin-users/lookup` + the `targetId` param | admin client regenerated; **hosts restarted on this branch first** and the endpoint verified present in `openapi/v1.json` before regenerating (the stale-host trap recorded in the T-0167/T-0168 plan) |

## Manual

| # | Check | Result |
|---|---|---|
| 13 | Real erase-screen round trip: look up by email, confirm the server identity, in-flight pre-disable, already-erased state | **Nešlo ověřit v této session** — needs an authed admin session; behaviour is pinned by handler + integration tests, flagged rather than claimed |

## Notes
- The frontend's client-side `getAdminOrderAuditTrail` filter is **deleted**, not merely
  supplemented — leaving it would keep the misleading pagination counts.
- The erase screen's separate `userHasInFlightOrders` probe is gone: the lookup carries the count,
  so there is one round trip and one source of truth.
- `InFlightStates` is duplicated in `Infra.Database` rather than referenced from
  `Core.AppServices` (layering, CLAUDE.md §2.1). Drift there only softens a UI pre-check; the
  erase command stays authoritative. Noted in code.
