# Test plan — T-0169 Redirect continuity sweep

Executed 2026-08-22 on `feat/T-0169-redirect-continuity`. Findings: AUTH-M3/M4/M5/L2/L3, PUB-L7, CUST-M6.

## Automated (all green)

| # | Check | Where |
|---|---|---|
| 1 | A redirect owned by another audience is dropped instead of bouncing a fresh session onto the "already signed in" panel (AUTH-M3) | `redirect-continuity.test.ts` |
| 2 | An owned redirect keeps its **query string** (`?state=Shipped&page=3`) | same |
| 3 | Public targets survive for both audiences; no redirect → audience home | same |
| 4 | `hostToAudience` bridges the login form's host-talk to the redirect helpers' audience-talk | same |
| 5 | `loginHrefWithRedirect` / `buildLoginRedirect` encode the destination (AUTH-L2, AUTH-M5) | same |
| 6 | Open-redirect guard still rejects absolute + protocol-relative targets | same |
| 7 | Suites | `npx tsc --noEmit` clean; vitest **213/213** (39 files, **+7 new**) |

## Code-reviewed (no automated pin — stated honestly)

| # | Change | Why no test |
|---|---|---|
| 8 | Middleware puts `pathname + search` into `?redirect=` (PUB-L7 / CUST-M6) | edge middleware; the composition it feeds is unit-tested (row 2) |
| 9 | Login success routes through `continueHref` (AUTH-M3) | the decision function is tested; the wiring is one call |
| 10 | Dual-host `auth.forbidden` shows admin-account copy + a link to `/admin/login` (AUTH-M4) | copy + link render |
| 11 | Register / verify / reset login links and magic-link landing preserve `?redirect=` (AUTH-L2) | href threading; builder tested |
| 12 | `/admin/login` calls `router.refresh()` after success (AUTH-L3) | one-line parity with the customer form |
| 13 | Shared `redirectToLogin` / `buildLoginRedirect` for terminal 401s (AUTH-M5) | builder tested; the navigation itself is a router call |

## Regression found while testing
`useSearchParams()` returns **null** outside a router context, which the new register-form read hit
in the existing T-0162 suite. Fixed in production code (optional read) rather than only in the test
mock — the null case is real in Next, not a test artifact.

## Manual

| # | Check | Result |
|---|---|---|
| 14 | Real deep-link → login → land back on the filtered page; admin on `/login`; magic link with a redirect | **Nešlo ověřit v této session** — needs an authed browser session; flagged, not claimed |

## Not done in this ticket
- **AUTH-L3 already-signed-in panel for `/admin/login`** — deferred: the customer panel depends on
  `getDisplaySession()` + `SwitchAccountButton`, both audience-scoped; wiring an admin variant is
  its own slice. The `router.refresh()` half of L3 ships here.
