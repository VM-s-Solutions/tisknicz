# Test plan — T-0175 Admin list infrastructure + route resilience

Executed 2026-08-22 on `feat/T-0175-admin-list-infrastructure`. Findings: ADM-H3/H4/M1/M2/M3/L1/L5/L7.

## Automated (all green)

| # | Check | Where |
|---|---|---|
| 1 | Shared pagination renders the page indicator every consumer now shares (the inline order-detail copy had none) | `admin-list-infrastructure.test.tsx` |
| 2 | Filters preserved across page links; `page=1` dropped from the query | same |
| 3 | Custom `pageParam` (`auditPage`) for the order-detail pager | same |
| 4 | Single page renders nothing | same |
| 5 | `parsePage` clamps `9007199254740991` → `ADMIN_MAX_PAGE`, junk/0/negative → 1 (ADM-L5) | same |
| 6 | `parsePageSize` clamps to the ceiling, honours a caller max, falls back on junk | same |
| 7 | `retryHref` rebuilds the current URL with filters + page (ADM-M3) | same |
| 8 | Suites | `npx tsc --noEmit` clean; vitest **200/200** (37 files, +7 new) |

## Structural verification

| # | Check | Evidence |
|---|---|---|
| 9 | Five pagination copies gone | `orders/pagination.tsx`, `faktury/pagination.tsx`, `audit/pagination.tsx`, `ops-pagination.tsx` deleted; inline `AuditPagination` removed — `grep` for the old imports returns 0 and tsc passes (any missed consumer would fail the build) |
| 10 | Unauthorized redirect parity | `redirect('/admin/login?redirect=…')` now present on makers + kategorie |
| 11 | Boundaries added | `loading.tsx` + `error.tsx` for kategorie, overview segment, `orders/[orderId]` |

## Manual

| # | Check | Result |
|---|---|---|
| 12 | Browser pass on the six admin lists (Chrome + WebKit, 375/768/1280), expired-session redirect, forced SSR throw | **Nešlo ověřit v této session** — needs an authed admin session; behaviour is pinned by unit tests + tsc, flagged rather than claimed |

## Notes
- The makers search form became a **Server** Component (native GET), removing a client island.
- `spacing` prop keeps the ops lists' tighter margin so the consolidation is visually neutral.
