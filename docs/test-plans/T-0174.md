# Test plan — T-0174 Maker product + review form feedback

Executed 2026-08-21 on `feat/T-0174-maker-form-feedback`. Findings: MAKER-H1/H2/M1/M2/M6-fe/M7/L6.

## Automated (all green)

| # | Check | Where |
|---|---|---|
| 1 | Reply form re-enables + collapses after successful submit (the MAKER-H1 regression); collapsed state for existing replies; edit/cancel toggle; failure re-enables | `recenze/__tests__/reply-form.test.tsx` (4 tests) |
| 2 | SaveButton disabled until dirty; success confirmed at the button; validation failure scrolls+focuses first errored field **after** inputs re-enable (focusing a disabled input is a no-op — the effect-based fix is pinned); create pushes `?created=1` | `produkty/__tests__/product-form-feedback.test.tsx` (4 tests) |
| 3 | Image manager: cap counter + disabled at 10/10; sequential queue (3 files → 3 uploads, 1 refresh); over-capacity skip notice; per-file failure listing by name | `produkty/__tests__/image-manager.test.tsx` (4 tests) |
| 4 | `uploadProductImage` passes `timeoutMs: 120_000` | pinned by the helper source (constant in call); behavior covered by the queue tests through the mocked helper |
| 5 | Suites | `npx tsc --noEmit` clean; vitest **185/185** (34 files) |

## Manual

| # | Check | Result |
|---|---|---|
| 6 | Real multi-MB photo upload over a slow uplink completing past 8 s, rendering afterwards (Chrome + WebKit); 375/768/1280 passes on the touched maker routes | **Nešlo ověřit v této session** — vyžaduje přihlášenou maker session v reálném prohlížeči; ověřit při nejbližším ručním průchodu (upload-timeout logika je totožná s ověřeným avatar/logo vzorem z profile.ts, který touto cestou už prošel) |

## Notes
- The scroll/focus fix uncovered a real bug during testing: focusing inside `applyError` no-ops
  because inputs are still disabled mid-submit; focus now runs in an effect after the errored
  render commits.
- Product-grid activity filter is a display filter over the fetched page (backend read has no
  is-active param yet) — counts/pagination remain those of the unfiltered set; backend param noted
  as follow-up in the ticket.
