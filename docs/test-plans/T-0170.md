# Test plan — T-0170 Katalog filter/pagination state integrity

Executed 2026-08-21 on `feat/T-0170-katalog-state-integrity`. Findings: PUB-H1/H2/H3/M3/M7.

## Automated (all green)

| # | Check | Where |
|---|---|---|
| 1 | Filter changes and reset **push** (never `replace`) — back undoes steps; URL contract (repeated `category`, page reset, stale-param dropping) re-pinned on the push path | `catalog-filters.test.tsx` (updated + new reset-hidden test) |
| 2 | Reset control hidden while nothing is filtered (panel parity) | `catalog-filters.test.tsx` |
| 3 | Pagination: plain click pushes the target URL with filter query preserved; modified click (new tab) neither pushes nor scrolls; scroll behavior + reduced-motion pinned | `catalog-pagination.test.tsx` (updated) |
| 4 | Three-way empty classification (out-of-range vs filtered vs genuinely empty) | `catalog-empty-kind.test.ts` (new) |
| 5 | Suites | `npx tsc --noEmit` clean; vitest **190/190** (35 files) |

## Manual (running app, SSR via curl)

| # | Check | Evidence |
|---|---|---|
| 6 | `/katalog` renders | 200 |
| 7 | `?page=999` → out-of-range surface | "Tato stránka výsledků už neexistuje" + "Na první stránku" in the SSR payload |
| 8 | `?city=Neexistující…` → filtered-to-zero surface | "Žádní výrobci neodpovídají vašemu filtru" + "Vymazat filtry" |
| 9 | Pending dim, URL→panel remount sync, scroll-after-data in a real browser (Chrome + WebKit, 375/768/1280) | **Nešlo ověřit v této session** (no driven browser); interaction logic pinned by the unit tests; verify at the next manual pass |

## Notes
- New shared primitives: `components/shared/navigation-transition.tsx` (provider + `TransitionDim` +
  hook with providerless fallback) and `components/shared/refresh-button.tsx` (`router.refresh()`
  retry) — both intentionally reusable by the remaining Phase 8 tickets (T-0171, T-0173, T-0175).
- Maker-profile product panel unified to the catalog's 300 ms debounce; genuinely-empty catalog CTA
  targets `/pro-makery`.
