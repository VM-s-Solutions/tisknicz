---
ticket: T-0084b
author: QA
created: 2026-06-10
---

# Test plan — T-0084b (stub — filled before bundle PR review)

## Scope under test
Pre-payment order page at `/objednavka/[id]`: SSR breakdown render, explicit
"Zaplatit" → Comgate redirect, payment error mapping + refresh-on-conflict,
attachment retry manager, `?attachmentsFailed` handoff, non-PendingPayment
banner, 404/login redirects.

## Preconditions
- Vercel preview against staging backend + Comgate sandbox (`Comgate:TestMode = true`).
- Order created via the T-0084a flow (PendingPayment).
- One order force-advanced to `Paid` on staging for the banner case.
- A second customer account for the foreign-order 404 case.

## Manual cases
Format: ID | Steps | Expected | Actual | Pass/Fail

| ID | Steps | Expected | Actual | Pass/Fail |
|---|---|---|---|---|
| TC-1 | Open a fresh PendingPayment order | Order number, state badge, breakdown (product/shipping/VAT %/total via formatCzk), contact snapshot, pay CTA, attachment manager, 24h deadline (createdAt + 24h, Czech format); no client fetch on load | | |
| TC-2 | Inspect network on page load | Zero `payment-session` requests (session created on click only) | | |
| TC-3 | Click "Zaplatit" | One POST; full navigation to Comgate; button disabled from click; rapid double-click fires one POST | | |
| TC-4 | Browser back from Comgate, click "Zaplatit" again within 24h | Redirect works again (backend cached/verified session; no frontend special-casing) | | |
| TC-5 | Force each `payment.*` error on staging | Czech alert via the matching i18n key; CTA re-enables | | |
| TC-6 | Pay the order in another tab, then click "Zaplatit" here | `order.paymentAlreadyCaptured` alert + page refreshes into the state banner | | |
| TC-7 | Order with 2 attachments | Both show filename + size; add-more allows at most 8 further files; type/size pre-checks reject before network | | |
| TC-8 | Fail an upload (offline), then "Zkusit znovu" | Same in-memory file re-uploads; success appends without full reload | | |
| TC-9 | Arrive with `?attachmentsFailed=2` | One-time warning alert prompting re-add | | |
| TC-10 | Open the `Paid` order | Minimal banner: Czech state label + "detail připravujeme" + catalog link; no pay CTA, no upload UI | | |
| TC-11 | Foreign order id / logged out | 404 page / login redirect with `redirect` param | | |
| TC-12 | Viewports 375 / 768 / 1280 | Layout holds; CTAs reachable | | |

## Automated cases
- None — no frontend test harness yet; manual + `tsc` + lint + build.

## Edge cases
- Auto-cancel fires while the page is open → pay click returns state-conflict
  → refresh → `Cancelled` banner.
- bfcache restore after returning from Comgate → stale view; pay click
  resolves via state-conflict refresh.

## Regression spot-checks
- T-0084a form still navigates here after create.
- Maker dashboard order surfaces unaffected.

## Defects found
—
