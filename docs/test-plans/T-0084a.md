---
ticket: T-0084a
author: QA
created: 2026-06-10
---

# Test plan — T-0084a (stub — filled before bundle PR review)

## Scope under test
Checkout order form at `/objednavka?productId=…`: entry guards, contact-field
validation mirrors, shipping-method gating (Packeta widget + personal pickup),
attachment pre-checks, submit orchestration (create → sequential uploads →
navigate to `/objednavka/<orderId>` with `?attachmentsFailed=<n>` on partial
failures).

## Preconditions
- Vercel preview against the staging backend.
- Confirmed customer account; an unconfirmed account for the 403 case.
- Active fixed-price product; an `OnRequest` product; an inactive product id.
- Maker WITH personal pickup enabled and maker WITHOUT it.
- Packeta public widget key configured on staging (T-0070 manual step
  `packeta-public-widget-key-secret`) — pre-condition, QA blocked without it.

## Manual cases
Format: ID | Steps | Expected | Actual | Pass/Fail

| ID | Steps | Expected | Actual | Pass/Fail |
|---|---|---|---|---|
| TC-1 | Visit with valid productId, logged in | Form + sticky summary; email prefilled; no client fetch on load (network panel) | | |
| TC-2 | Visit logged out | Redirect to `/auth/login?redirect=…`; after login lands back on the form | | |
| TC-3 | Visit without productId / unknown productId | Invalid-link state with catalog CTA / 404 page | | |
| TC-4 | Visit with `OnRequest` product | Redirect to `/produkt/<id>` | | |
| TC-5 | Submit with 1-char name, bad phone, 2001-char note | Czech field errors, no POST fired | | |
| TC-6 | Select Zásilkovna, open widget, choose a point | Widget opens; point name + "Změnit" shown; submit allowed | | |
| TC-7 | Submit Zásilkovna without a point | Czech error under the shipping section, no POST | | |
| TC-8 | Block the widget script (devtools) and select Zásilkovna | Option disabled + error notice + retry; retry re-attempts load | | |
| TC-9 | Maker without personal pickup | Radio disabled with tooltip/hint | | |
| TC-10 | Personal pickup selected | Pickup note + city shown; request carries `PersonalPickup`, no point id | | |
| TC-11 | Rapid double-click submit with 2 attachments | Exactly one `POST /orders`; sequential attachment POSTs with statuses; navigate to order page | | |
| TC-12 | Kill network during 2nd of 3 uploads | Navigation still proceeds with `?attachmentsFailed=1` | | |
| TC-13 | Pick an 11th file / 11-MiB file / .zip | Client-side Czech rejection, no network call | | |
| TC-14 | Submit as unconfirmed-email account | Form-level alert with `auth.emailNotConfirmed` copy + resend hint | | |
| TC-15 | Viewports 375 / 768 / 1280 | Single column with summary on top at 375/768; sticky summary at 1280 | | |

## Automated cases
- None — the frontend has no test harness yet (no test script in
  `frontend/package.json`). Coverage is manual + `tsc` + lint + build.

## Edge cases
- Widget config endpoint down at SSR time → Zásilkovna disabled, retry refreshes.
- Maker deactivated between page load and submit → `maker.deactivated` alert.
- Product deactivated between page load and submit → `product.notActive` alert.

## Regression spot-checks
- Product detail page "Objednat" CTA still links here.
- Catalog + product detail pages unaffected.

## Defects found
—
