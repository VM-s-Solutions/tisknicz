---
ticket: T-0085
author: QA
created: 2026-06-10
---

# Test plan — T-0085 (stub — filled before bundle PR review)

> 2026-06-10 fold: TC-11 added per the Gate 9 + QA audit fold list (F4 —
> SSR-arrival Cancelled, decision-matrix row 5).

## Scope under test
Payment confirmation page at `/objednavka/[id]/potvrzeni`: SSR decision
matrix, defensive `?status=` parsing, capped visibility-aware poll-to-Paid,
success granted only by backend state.

## Preconditions
- Vercel preview against staging backend + Comgate sandbox end-to-end.
- Manual step `comgate-return-urls-config` applied on staging (merchant
  return URLs → `https://<domain>/objednavka/{refId}/potvrzeni?status=<…>`).
- Confirmed customer with a PendingPayment order.

## Manual cases
Format: ID | Steps | Expected | Actual | Pass/Fail

| ID | Steps | Expected | Actual | Pass/Fail |
|---|---|---|---|---|
| TC-1 | Pay with sandbox test card, webhook fast | Verifying frame → success swap within ~1–2 polls, no reload | | |
| TC-2 | Pay, webhook delayed >30s (pause Function) | Cap-reached view at ~30s; polling stops permanently; order-paid email still arrives | | |
| TC-3 | Cancel at the gateway | Failure view immediately on return; zero polls (network panel); retry CTA lands on the T-0084b pay button | | |
| TC-4 | Revisit confirmation URL after order already Paid | Success view from SSR, no poller | | |
| TC-5 | Craft `?status=paid` on an unpaid order | Verifying frame only — success never renders from the param | | |
| TC-6 | Hide the tab mid-poll, return after 1 min | Polls paused while hidden; immediate poll on return; budget not consumed while hidden | | |
| TC-7 | Foreign order id / logged out | 404 page / login redirect with `redirect` param | | |
| TC-8 | Navigate away mid-poll | No timer leak (no further detail requests after navigation) | | |
| TC-9 | Viewports 375 / 768 / 1280 on all four frames | Layout holds; CTAs reachable | | |
| TC-10 | Poll observes `Cancelled` (auto-cancel races) | View flips to the failure frame | | |
| TC-11 | Arrive (SSR) at the confirmation URL for an order already `Cancelled`, with `?status=` absent or a non-failure value | State banner rendered from backend state (`orderStateLabelKey`); no poller started; success never shown | | |

## Automated cases
- None — no frontend test harness yet; manual + `tsc` + lint + build.

## Edge cases
- `?status=` absent / garbage / uppercase variants → optimistic verifying path.
- Order in `Refunded`/`Disputed` with non-failure status → state banner.

## Regression spot-checks
- /objednavka/[id] pay flow unaffected.
- Order detail link from the success frame lands on T-0084b's page.

## Defects found
—
