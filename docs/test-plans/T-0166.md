# Test plan — T-0166 Fix dead transactional email link paths

Executed 2026-08-21 on `feat/T-0166-email-link-paths` (local dev). Findings ref: AUTH-H1.

## Automated

| # | Check | Where | Result |
|---|---|---|---|
| 1 | Default path templates pinned exactly (`/verify`, `/magic`, `/reset` + `{token}`) | `PublicAppUrlsOptionsValidatorTests.Default_paths_target_the_real_frontend_routes` | pass |
| 2 | Composed absolute URLs incl. trailing-slash base + URL-escaped token (AC-1) | `PublicAppUrlsOptionsValidatorTests.Default_paths_compose_to_real_frontend_urls` | pass |
| 3 | Real composer (`EmailSendService.BuildActionUrl`) emits `…/magic?token=` with **code defaults** — fixture no longer overrides paths | `EmailSendServiceTests` (action_url + plain-text assertions) | pass |
| 4 | Legacy `/auth/{confirm,verify,magic,reset}` permanent redirects present with correct destinations (AC-2) | `frontend/src/__tests__/next-config-auth-redirects.test.ts` | pass |
| 5 | Suites (after review fold) | `Makables.Tests` **2079/2079**; frontend vitest **173/173** (31 files) | pass |
| 5b | Review-fold guard: redirect targets are real routes on disk (`(auth)/{verify,magic,reset}/page.tsx`) — proven to fire (an earlier wrong path resolution failed exactly this assertion) | `next-config-auth-redirects.test.ts` | pass |
| 6 | `Makables.IntegrationTests` | 84 pass / 227 fail locally — **all failures are `DockerUnavailableException`** (no Docker runtime on this machine, pre-existing; suite runs in CI with Docker) | env-limited |

## Manual (running app, Chrome; curl for status codes)

| # | Check | Evidence | Result |
|---|---|---|---|
| 7 | All four legacy paths 308 → live route, query preserved incl. encoded chars | `curl /auth/confirm?token=abc%2F1` → `308 /verify?token=abc%2F1` (same for verify/magic/reset) | pass |
| 8 | AC-3 round trip: register → mint token (dev recipe) → open composed URL | user `t0166-roundtrip@makables.test` registered on :5001; `GET /verify?token=<raw>` → 200; legacy `GET /auth/confirm?token=<raw>` -L → 200 at `/verify?token=<raw>`; `POST /api/v1/auth/confirm-email` → 200; DB `email_confirmed_at` `null → 2026-08-21T13:14:12Z` | pass |
| 9 | Stale-config trap avoided | `next dev` restarted before verification (next.config read once at boot) | done |

## Notes

- The browser-JS auto-fire of the confirm POST on `/verify` mount was exercised via the identical
  POST (row 8); the mount behavior itself is covered by the existing verify-client tests and is
  unchanged by this ticket.
- No NSwag regen (no contract change). No new i18n keys / error codes.
- 13 pre-existing `check-consistency` findings on master (none in touched files) — out of scope;
  CI baseline covers them (master CI green).
