# Test plan — T-0176 Admin action feedback + modal accessibility

Executed 2026-08-22 on `feat/T-0176-admin-action-feedback`. Findings: ADM-H6/M5/M6/M10/L2/L3/L4/L6.

## Automated (all green)

| # | Check | Where |
|---|---|---|
| 1 | Dialog moves focus inside on mount (ADM-M10 — the old shells left focus on the trigger behind the backdrop) | `dialog.test.tsx` |
| 2 | Tab wraps last→first; Shift+Tab wraps first→last | same |
| 3 | Focus that escaped to the page behind is pulled back in | same |
| 4 | Escape closes when idle, and is ignored while a request is in flight | same |
| 5 | jest-axe clean | same |
| 6 | Success notice survives the unmount of the row that reported it (ADM-M5) | `action-notice.test.tsx` |
| 7 | `useActionNotice` is a no-op outside a provider (rows render standalone) | same |
| 8 | Suites | `npx tsc --noEmit` clean; vitest **215/215** (40 files, **+9 new**) |

## Code-reviewed (no automated pin — stated honestly)

| # | Change | Why no test |
|---|---|---|
| 9 | Category row resets `busy` on the success path (ADM-H6) — the Edit button no longer bricks | one-line state fix; the row needs an authed admin page to render meaningfully |
| 10 | Refund / manual state change / complete-batch now raise a page-level success Alert (ADM-M6) | notice lives on the parent after the modal unmounts; verified by reading the flow, browser pass pending |
| 11 | Arm-confirm auto-disarms on Escape / after 8 s in both copies (ADM-L3) | timer + key handler in a shared hook |
| 12 | Fee-override form prefills the existing override (ADM-L4) | prop threading |
| 13 | Invoice + payout-CSV download errors carry the resolved message instead of a bare boolean (ADM-L6) | error-surface swap |

## Not done in this ticket

- **ADM-L2** (return-label card: shared busy spinner, generated label unreachable) — deferred; it
  needs a backend label-URL read to be useful, so it belongs with the T-0179 contract pass rather
  than a UI-only fix. Recorded here so it is not silently dropped.

## Manual

| # | Check | Result |
|---|---|---|
| 14 | Keyboard pass through all three modals, refund/state-change/complete-batch confirmations, outbox retry in a real admin session | **Nešlo ověřit v této session** — needs an authed admin session; flagged, not claimed |
