# Test plan — T-0173 Customer + maker dashboard polish

Executed 2026-08-22 on `feat/T-0173-dashboard-polish`.
Findings: CUST-M1/L2/L5, MAKER-M3(copy)/M4/L1/L2/L5.

## Automated (all green)

| # | Check | Where |
|---|---|---|
| 1 | Profile failure renders translated copy + retry and **never** the raw backend message (CUST-M1) | `profile-load-error.test.tsx` |
| 2 | Suites | `npx tsc --noEmit` clean; vitest **226/226** (43 files, +1 new) |

## Manual (running app)

| # | Check | Evidence |
|---|---|---|
| 3 | `/dashboard/maker` no longer 404s inside the maker's own chrome (MAKER-L1) | returns **307** to `/dashboard/maker/objednavky` |

## Code-reviewed (no automated pin — stated honestly)

| # | Change | Why no test |
|---|---|---|
| 4 | Both profile pages redirect to login on `Unauthorized` (CUST-M1) — parity with the orders page one route over | server-component redirect |
| 5 | Order-list retries rebuild the CURRENT url; a `Validation` failure (inverted dates) offers "clear filters" instead, which is the one case where resetting IS the fix (CUST-L2 / MAKER-L2) | href composition inside a server component |
| 6 | Payouts: bank-account-missing warning linking the profile + payout-cadence copy (MAKER-M4, MAKER-M3 copy half) | needs an authed maker with an empty bank account |
| 7 | Out-of-range maker page offers the last real page instead of an empty box under a nonzero count (MAKER-L5); deep-linked page bounded at 10 000 | server-render branch |
| 8 | Dispute escalation collapsed behind a "Reklamovat" disclosure (CUST-L5) | presentational |

## Not done in this ticket
- **CUST-L4** (link order rows to the product / maker) — **cannot** be done here: neither
  `CustomerOrderListItemDto` nor `CustomerOrderDetailDto` carries `productId` or `makerSlug`, only
  the display names. It needs a backend contract change and moves to the **T-0179** grooming pass.
  Verified against the generated client rather than assumed.
- **MAKER-L3** (per-keystroke date pushes / inverted-range 400) and **MAKER-L4** (no state filter on
  the "Vše" tab, no order search) — deferred as LOW; the inverted-range case now at least offers
  "clear filters" (item 5) instead of stranding the maker.

## Manual gap
| # | Check | Result |
|---|---|---|
| 9 | Authed customer + maker pass at 375/768/1280 | **Nešlo ověřit v této session** — no authed browser session; flagged, not claimed |
