# Test plan — auth-recovery bundle (T-0167 + T-0168)

Executed 2026-08-22 on `feat/auth-recovery-bundle`. Findings: AUTH-H2/H3/M1/M2/M6/L4, CUST-H2.

## Automated — backend

| # | Check | Where | Result |
|---|---|---|---|
| 1 | Replay of a token consumed within the 24 h grace, user already confirmed → **Success** (AUTH-M1) | `ConfirmEmailHandlerTests` | pass |
| 2 | Replay outside the grace → invalid; replay for an *unconfirmed* user → invalid (no lying) | `ConfirmEmailHandlerTests` | pass |
| 3 | `Makables.Tests` full suite | **2082/2082** (+3 new) | pass |
| 4 | Google callback with invalid state → **302** to `/login?oauth_error=…`, no `@`/`code=`/`state=` in Location (AUTH-H2) | `AuthRecoveryIntegrationTests` | compiles; runs in CI (Docker/Testcontainers unavailable on this machine) |
| 5 | `resend-confirmation` answers byte-identically for a known vs unknown email (no enumeration) | `AuthRecoveryIntegrationTests` | compiles; runs in CI |

## Automated — frontend

| # | Check | Where | Result |
|---|---|---|---|
| 6 | `VerifyClient` fires the one-time confirm **exactly once** under StrictMode double-mount (AUTH-M1) | `auth-recovery.test.tsx` | pass |
| 7 | Verify failure card offers login + resend (no dead end) | same | pass |
| 8 | Magic consume retries the **maker** host on `auth.forbidden` and lands on the maker dashboard (AUTH-H3) | same | pass |
| 9 | Both-hosts rejection shows owned Czech copy + "Poslat nový odkaz"/login links | same | pass |
| 10 | Resend form: uniform success copy; a failed resend surfaces its error (AUTH-L4) | same | pass |
| 11 | `npx tsc --noEmit` clean; vitest **199/199** (37 files, +6 new) | — | pass |

## Contract

| # | Check | Result |
|---|---|---|
| 12 | NSwag regen for the new `POST /auth/resend-confirmation` | All 4 clients regenerated. **Note:** the first regen attempt silently produced a stale spec because the long-running dev hosts predated the branch — hosts were restarted from this branch and the endpoint verified present in `openapi/v1.json` before re-running. |

## Manual

| # | Check | Result |
|---|---|---|
| 13 | Live Google OAuth round trip (needs real Google credentials); real maker magic-link email; profile banner in a browser at 375/768/1280 | **Nešlo ověřit v této session** — no OAuth credentials and no authed browser session here; the 302 shape + no-PII rule are pinned by integration tests instead |

## Notes
- `EmailConfirmationBanner` previously sent a magic **link** while its copy promised a confirmation
  email — it now calls the dedicated resend endpoint.
- The banner was implemented but mounted nowhere; it is now on the customer profile, which is
  exactly where checkout's error copy points (closes CUST-H2).
