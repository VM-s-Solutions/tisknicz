# Test plan — T-0172 Checkout + pre-payment flow usability

Executed 2026-08-21 on `feat/T-0172-checkout-flow-usability`. Findings: CUST-H1/H3/H4/M2/M4/M5/L1/L3.

## Automated (all green)

| # | Check | Where |
|---|---|---|
| 1 | Profile name/email/phone arrive as editable defaults (CUST-H4) | `order-form-usability.test.tsx` |
| 2 | Failed mirror validation scrolls to + focuses the first errored field (CUST-H3) | `order-form-usability.test.tsx` |
| 3 | Editing a field clears its stale error immediately (CUST-L1) | `order-form-usability.test.tsx` |
| 4 | Suites | `npx tsc --noEmit` clean; vitest **193/193** (36 files, +3 new) |

## Code-reviewed (no automated pin — noted honestly)

| # | Change | Why no test |
|---|---|---|
| 5 | Attachment rows use `FileDownloadButton` (blob via apiFetch) instead of the raw `<a href={backend-relative}>` that 404'd (CUST-H1) | rendering swap; behavior of `FileDownloadButton` already covered by its own usage; runtime download needs an authed browser session |
| 6 | Confirmation page: terminal state now beats the `?status=` failure param — an already-cancelled order can no longer promise a 24 h pay button (CUST-M2); `detailComing` copy replaced with a real detail link | server-component branch ordering; pinned by the inline comment + review |
| 7 | Poller performs one final poll at the cap before the "still verifying" frame (CUST-M5) | 5-minute fake-timer harness judged not worth the flake risk; logic is 6 lines with an idempotent setView guard |
| 8 | `beforeunload` guard while the checkout form is dirty (CUST-M4); stale `?attachmentsFailed=N` stripped on mount (CUST-L3) | browser-native prompt / URL side effect |

## Manual

| # | Check | Result |
|---|---|---|
| 9 | Real checkout round trip at 375/768/1280 (Chrome + WebKit): failed submit lands feedback in-viewport; attachment downloads a real file on the pre-payment page | **Nešlo ověřit v této session** (needs an authed browser session) — verify at the next manual pass |

## Notes
- The unconfirmed-email notice block in `order-form-client.tsx` was deliberately NOT touched — it
  belongs to T-0168 (resend-confirmation flow).
