# Test plan — T-0171 Public error surfaces + navigation context

Executed 2026-08-22 on `feat/T-0171-public-error-navigation`.
Findings: PUB-M1/M2/M4/M5/M6, PUB-L3/L4/L6/L8.

## Automated (all green)

| # | Check | Where |
|---|---|---|
| 1 | `(public)/error.tsx` renders Czech copy, a working `reset()` retry and a way onward (PUB-M1) | `public-error-navigation.test.tsx` |
| 2 | Root `error.tsx` stands alone without the public chrome (it may be the chrome that failed) | same |
| 3 | jest-axe clean on the boundary | same |
| 4 | Any signed-in **non-customer** audience gets the explanation instead of the login loop — widened from maker-only (PUB-L6) | `fulfillment-badge.test.tsx` (updated) |
| 5 | Suites | `npx tsc --noEmit` clean; vitest **225/225** (42 files, **+3 new**) |

## Manual (running app, SSR)

| # | Check | Evidence |
|---|---|---|
| 6 | `/katalog` renders | 200 |
| 7 | A URL naming a category that no longer exists shows the stale-category notice instead of silently returning the full catalog (PUB-M5) | `?category=neexistujici-kategorie` → "Tato kategorie už v katalogu není" in the SSR payload |

## Code-reviewed (no automated pin — stated honestly)

| # | Change | Why no test |
|---|---|---|
| 8 | Product back-link is now a breadcrumb returning to the owning maker (PUB-M2) | link href composition |
| 9 | Reviews badge shows the true `ratingCount`, not the capped list length (PUB-M4) | prop threading; the contradiction was between two numbers on one screen |
| 10 | Landing tiles read the live category list, falling back to the launch six (PUB-M5) | server read + presentation map |
| 11 | Logout failure surfaces inline instead of failing silently (PUB-M6) | needs a failing logout round trip |
| 12 | ScrollToTop on the maker profile (PUB-L3); retry beside the back link on maker + product error surfaces (PUB-L4); `/produkt/*` marks the catalog nav active (PUB-L8) | presentational |

## Not done in this ticket
- **PUB-L1** (root 404 without site navigation) and **PUB-L2** (outdated loading skeletons) —
  deferred: L1 needs the root `not-found` to resolve the session-aware navbar, which is exactly the
  dependency the root boundary deliberately avoids; L2 is cosmetic drift best fixed alongside the
  next layout change. Both remain open findings, recorded rather than silently dropped.

## Manual gap
| # | Check | Result |
|---|---|---|
| 13 | Chrome + WebKit at 375/768/1280 on the touched public pages; a forced render throw in a browser | **Nešlo ověřit v této session** — no driven browser here; flagged, not claimed |
