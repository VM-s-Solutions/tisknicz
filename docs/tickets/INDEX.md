# Backlog manifest

PM owns this file. Updates on every state change.

This manifest lists every ticket as a single row with metadata. **When a ticket moves to `ready`, PM expands it to a full `T-NNNN-<slug>.md` file** using `template.md`. The full file carries the AC, scope, test plan reference, and status log.

Until expanded, the manifest row is the lightweight backlog representation. Sprint plans are at the bottom of this file.

## Legend

- **Phase**: implementation phase (1 = scaffold; 2 = identity; 3 = catalog; 4 = orders; 5 = post-order; 6 = polish)
- **Size**: S (<4h), M (4–16h), L (>16h — must split before `ready`)
- **State**: draft | ready | in_progress | in_review | qa | done | blocked
- **Owner**: `dotnet-backend` / `dotnet-db` / `frontend` / `secops` / `architect` when in_progress

---

## Phase 1 — Foundation scaffold (sequential, blocking everything)

| Ticket | Title | Phase | Size | State | Depends on | Stories | ADRs |
|---|---|---|---|---|---|---|---|
| T-0001 | Scaffold .NET solution skeleton — Core.Domain, Core.AppServices, Config, Infra.*, Web.*, Functions, Tests | 1 | M | **done** | — | — | 0001, 0007, 0008 |
| T-0002 | EF Core: MakablesDbContext, audit interceptor, global query filter for soft-delete | 1 | M | **done** | T-0001, T-0004, T-0006 | — | 0011, 0013, 0014 |
| T-0003 | Wire MediatR + FluentValidation + pipeline behaviors (Validation + UnitOfWork) | 1 | S | **done** | T-0001, T-0004, T-0002 | — | 0002 |
| T-0004 | Shared types: BusinessResult, Error, ErrorType, BusinessErrorMessage; ICommand / IQuery markers; MakablesApiController base | 1 | S | **done** | T-0001 | — | 0002 |
| T-0005 | Money value object + MoneyFormatter + tests | 1 | S | **done** | T-0001 | — | 0003 |
| T-0006 | Auditable base entity + IClock + IIdGenerator + IUserSessionProvider (interceptor wiring moved to T-0002) | 1 | S | **done** | T-0001, T-0004 | — | 0011, 0013, 0014 |
| T-0007 | NumberingSequence table + IOrderNumberGenerator / IInvoiceNumberGenerator / IPayoutBatchNumberGenerator with FOR UPDATE lock | 1 | M | **done** | T-0001, T-0002, T-0006 | — | 0009 |
| T-0008 | DI wiring: AddMakablesInfrastructure / AddMakablesAuth / AddMakablesCors / AddMakablesMediator / AddMakablesClients / AddMakablesRateLimiting | 1 | M | **done** | T-0001, T-0002, T-0003, T-0006, T-0007 | — | 0008 |
| T-0009 | Four Web hosts (Customer / Maker / Admin / Public) sharing Config; per-host Program.cs; per-host CORS + rate limit | 1 | M | **done** | T-0008 | — | 0005, 0008 |
| T-0010 | Country + CountryConfiguration entity + seed migration (CZ row); ICountryConfigurationRepository | 1 | M | **done** | T-0002, T-0006, T-0007, T-0008, T-0009 | — | 0004, 0013 |
| T-0011 | Outbox table + IOutbox producer helper + OutboxRepository; AdminAuditLogEntry table + AdminAuditPipelineBehavior | 1 | M | **done** | T-0002, T-0003, T-0006, T-0008, T-0010 | — | 0014, 0020 |
| T-0012 | API versioning wiring (Asp.Versioning.Mvc); URL-path versioning; openapi/v1.json per host | 1 | S | **done** | T-0009 | — | 0021 |
| T-0013 | NSwag pipeline: nswag/config.json; npm run generate:api; CI spec-hash parity check; pre-commit hook blocking manual edits to api-client/ | 1 | M | **done** | T-0009, T-0012 | — | 0022 |
| T-0014 | Structured logging (Serilog), OpenTelemetry traces + metrics via .AddServiceDefaults() (Aspire); Application Insights wiring | 1 | M | **done** | T-0009 | — | 0023 |
| T-0015 | Frontend scaffold: route groups (public, auth, customer, maker, admin); api-client/ folder structure; api-fetch wrapper; lib/runtime Result type; lib/i18n/cs-CZ catalog scaffold | 1 | M | **done** | T-0013 | — | 0005, 0022 |
| T-0016 | Deploy: Bicep templates for Azure (Postgres Flexible Server, four App Services, Functions, Blob, Key Vault, App Insights); staging + production environments; deploy pipeline GitHub Actions | 1 | L | **done** | T-0014 | — | 0023 |

**Phase 1 total:** 16 tickets. Sequential where dependencies dictate; T-0014 / T-0015 / T-0016 can parallelize at the end.

---

## Phase 2 — Identity (auth, users, makers)

| Ticket | Title | Phase | Size | State | Depends on | Stories | ADRs |
|---|---|---|---|---|---|---|---|
| T-0020 | User + RefreshToken entities + migrations; IUserRepository + IRefreshTokenRepository | 2 | M | draft | Phase 1 done | — | 0012, 0013 |
| T-0021 | IPasswordHasher (Argon2id); IJwtIssuer (HS256 + audience); Argon2 + JwtIssuer impls; tests | 2 | M | draft | T-0020 | — | 0012 |
| T-0022 | IAuthService + AuthService impl: Register, Login, Refresh, Logout, lockout policy | 2 | L | draft | T-0021 | US-customer-0001, 0002, 0019, 0020; US-maker-0002, 0016; US-admin-0001, 0017 | 0012 |
| T-0023 | Magic-link flow: SendMagicLinkAsync + ConsumeMagicLinkAsync; email template `magic-link/cs-CZ` | 2 | M | draft | T-0022, T-0029 | US-customer-0003 | 0012, 0019 |
| T-0024 | Email confirmation flow: SendEmailConfirmation + ConfirmEmail; email template `welcome-customer/cs-CZ`, `welcome-maker/cs-CZ`; gate order placement on confirmed email | 2 | M | draft | T-0022, T-0029 | US-customer-0005 | 0012, 0019 |
| T-0025 | Password reset flow: SendPasswordResetAsync + ConfirmPasswordResetAsync; email template `reset-password/cs-CZ` | 2 | M | draft | T-0022, T-0029 | US-customer-0006 | 0012, 0019 |
| T-0026 | Google OAuth: GoogleOAuthClient + CompleteGoogleOAuthAsync; audience-bound state; reject for admin audience | 2 | M | draft | T-0022 | US-customer-0004 | 0012 |
| T-0027 | Auth middleware per host: JWT validation, audience enforcement, role check | 2 | M | done | T-0021, T-0009 | — | 0005, 0012 |
| T-0028 | Email pipeline: SendGrid Dynamic Templates + DB-backed translation (EmailTemplate + EmailTemplateTranslation) + ILanguageResolver + outbox payload carries LanguageCode; ADR 0019 amended Resend→SendGrid; bounce webhook deferred | 2 | L | done | T-0011, T-0020, T-0023, T-0024, T-0025 | — | 0019 |
| T-0029 | Outbox processor Function: ProcessOutboxFunction (timer 30s + HTTP) routes to send-email queue → SendEmailFunction calls IEmailSendService; OutboxRetryPolicy (1m→5m→15m→1h→6h→24h, stall after 6); OutboxEvent.ParkPendingConsumer; queue-message body = bare outbox id (payload stays in Postgres) | 2 | M | done | T-0011, T-0028 | — | 0020 |
| T-0030 | Address entity + IAddressRepository + IAddressFormatValidator (reads CountryConfiguration.ZipFormat); ConfigurationDrivenAddressFormatValidator with regex cache + timeout; AddressZipRules FluentValidation mixin | 2 | S | done | T-0010 | — | 0010 |
| T-0031 | IAddressGeocoder + MapboxAddressGeocoder; backend autocomplete proxy (Customer + Maker hosts via shared Config controller); partitioned per-user rate-limit policy `addresses-autocomplete` (20/min/user, 5/min/IP for unauth); Polly retry 2x; non-blocking failure per ADR 0010 §"Geocoding policy" | 2 | M | done | T-0030 | — | 0010 |
| T-0032 | ICompanyRegistry + AresCompanyRegistry; CzechIcoValidator mod-11; company_registry_cache (24h TTL + 7-day stale fallback); IMemoryCache hot layer; shared Polly ResiliencePipelineRegistry; CzechLegalForms map | 2 | M | done | T-0030 | — | 0018 |
| T-0033 | Maker entity (Auditable; snapshot of ARES fields + IsVerified admin gate); IMakerRepository (Add + IcoExistsAsync + GetByUserIdAsync); RegisterMaker command — 6-step flow: IČO format gate → ARES lookup → dissolved-entity reject (MakerCompanyDissolved) → email/IČO conflict pre-checks → atomic User+Address+Maker add → email-confirmation token (shared issuer). Stale ARES snapshot surfaces on Response.SnapshotIsStale (non-blocking per ADR 0018). | 2 | L | done | T-0020, T-0030, T-0032 | US-maker-0001 | 0010, 0012, 0018 |
| T-0034 | UpdateMakerProfile (maker self-service: bio, bankAccount, personalPickupEnabled, pickupNote — IDOR-shielded; user resolved from session) + admin VerifyMaker + DeactivateMaker + RefreshMakerFromAres commands (all IAdminAuditableCommand → before/after JSONB via AdminAuditPipelineBehavior). Adds CzechBankAccountValidator (ČNB mod-11) + Address.Update mutator (clears coordinates so the geocoder sweep refills). | 2 | M | done | T-0033 | US-maker-0003, 0015; US-admin-0003, 0004, 0005 | 0014 |
| T-0035 | Backend AuthController (9 endpoints: register/login/logout/refresh/confirm-email/{request,confirm}-password-reset/{request,consume}-magic-link) on every host via shared Config; RegisterMakerController on Public host only; AuthCookies helper (HttpOnly+Secure+SameSite=Strict, makables_access_{aud}/makables_refresh_{aud}); IHostAudience per Web host. Frontend pages /auth/login, /auth/register, /auth/register/maker, /auth/verify, /auth/reset, /auth/magic + email-confirmation banner; lib/api-client-helpers/auth.ts hand-written wrappers; apiFetch defaults to credentials:'include'. NSwag regen deferred. | 2 | L | done | T-0024, T-0025, T-0023, T-0026, T-0033 | US-customer-0001–0006; US-maker-0001, 0002 | 0012, 0005, 0008 |
| T-0036 | Backend GetMyProfile / UpdateUserProfile / ChangePassword (Auth feature) + GetMyMakerProfile (Maker feature); ProfileController under /api/v1/me (5 endpoints, [Authorize], shared across hosts). Frontend /dashboard/zakaznik/profile (3 sections: personal info / password / logout) and /dashboard/maker/profil (read-only ARES snapshot + verification badge + stale-snapshot banner + editable bio/bankAccount/personalPickupEnabled/pickupNote). Categories + pickup-address deferred. | 2 | M | done | T-0034, T-0035 | US-customer-0018, 0019, 0020; US-maker-0003, 0015, 0016 | 0012, 0014, 0018 |

**Phase 2 total:** 17 tickets.

---

## Phase 3 — Catalog (products, browse)

| Ticket | Title | Phase | Size | State | Depends on | Stories | ADRs |
|---|---|---|---|---|---|---|---|
| T-0040 | Category entity (Auditable + Slugify NFD-decompose Czech diacritics) + ICategoryRepository (Add / GetByIdAsync / SlugExistsAsync) + partial unique index ix_categories_slug WHERE is_active + UniqueConstraintTranslator race-translation; migration seeds 6 launch categories (cat-3d-tisk, cat-klasicky-tisk, cat-potisk-textilu, cat-laser-cnc, cat-velkoformat, cat-handmade) + maker_categories join (composite PK, no domain entity); admin CreateCategory / UpdateCategory (rename without touching slug per US-admin-0013 AC-2) / DeactivateCategory (all IAdminAuditableCommand, fail-closed on missing session). | 3 | M | done | T-0010 | US-admin-0013 | 0014 |
| T-0041 | Product entity (Auditable; Money pair + PriceType Fixed/From/OnRequest; owned ProductImage collection, ≤10 cap; currency immutable) + IProductRepository; CreateProduct (tenant currency from CountryConfiguration, maker from session) / UpdateProduct / DeleteProduct (soft) / AddProductImage / RemoveProductImage (best-effort blob delete), all IDOR-shielded by maker ownership. ImageUploadValidator (≤5MB, jpeg/png/webp, magic-byte sniff). Web.Maker ProductController (CRUD + multipart upload → product-images blob) + Web.Public ProductImageController (anonymous ETag-cached streaming). | 3 | L | done | T-0040, T-0042 | US-maker-0004 | 0003, 0011 |
| T-0042 | IBlobStorageClient (`Core.Domain/Storage/`) + AzureBlobStorageClient — `BusinessResult<T>` surface, no exceptions cross the boundary; container allow-list rejects unknown names (would default to private and break public product-images); conservative path safety (no leading `/`, `\\`, `.`/`..` segments, ≤1024 chars); 404 → `BlobNotFound`, other RequestFailedException → `Blob{Upload,Download}Failed` Transient. AzureBlobStorageOptions ValidateOnStart requires `ConnectionString` (dev/CI/Azurite) OR https `ServiceUri` (Managed Identity / DefaultAzureCredential). HTTP file-streaming endpoints deferred to per-feature tickets (T-0041 product images, etc.). | 3 | M | done | T-0001 | US-customer-0017; US-maker-0009, 0013; US-admin-0012 | 0011 |
| T-0043 | GetPagedMakers catalog query (ICatalogQueries read-side interface in Core.Domain + AsNoTracking projection in Infra; Maker→User→Address join; publicly-listable gate = active + email-confirmed; category/city(partial,ci)/min-rating filters; rating-desc→orders-desc sort; PagedData<T>). Adds deferred Maker fields: Slug (SlugGenerator, immutable across ARES refresh, unique partial index) + RatingAverageBp/RatingCount/TotalOrders (denormalized, SetCatalogStats) + ix_makers_catalog_sort. MakerCategory join entity maps the T-0040 table. RegisterMaker slug disambiguation. Web.Public CatalogController. | 3 | M | done | T-0033, T-0041 | US-customer-0007 | — |
| T-0044 | GetMakerBySlug query (ICatalogQueries; MakerProfile DTO = header + active products newest-first w/ primary image + empty Reviews deferred to T-0050; same publicly-listable gate as the list so hidden makers arent probeable by slug). Web.Public GET /api/v1/catalog/makers/{slug}. | 3 | M | done | T-0033, T-0041, T-0043 | US-customer-0008 | — |
| T-0045 | GetProductById query (ICatalogQueries; ProductDetail DTO = product fields + ordered images + owning-maker display info; null when product inactive or maker not listable). Web.Public GET /api/v1/catalog/products/{productId}. | 3 | S | done | T-0041, T-0043 | US-customer-0009 | — |
| T-0046 | Frontend: /katalog page (filters, list, pagination) | 3 | M | done | T-0043, T-0046b | US-customer-0007 | — |
| T-0046b | Public Web.Public.CatalogController [ProducesResponseType] annotations + canonical dev port move 5004→5104 + first NSwag regen of public-api.v1.ts with typed return shapes. Surfaced and worked around the model-binding ValidationProblemDetails vs domain Error 400 ambiguity on GetMakers. | 3 | S | **done** | T-0043, T-0044, T-0045 | — | — |
| T-0047 | Frontend: /katalog/[slug] maker profile page | 3 | M | **done** | T-0044, T-0046, T-0046b | US-customer-0008 | — |
| T-0048 | Frontend: /produkt/[productId] product detail page | 3 | M | **done** | T-0045, T-0047 | US-customer-0009 | — |
| T-0049 | Frontend: /dashboard/maker/produkty (CRUD UI; image picker; price/weight forms) | 3 | L | **done** | T-0041, T-0042, T-0049a, T-0049b | US-maker-0004 | 0024 |
| T-0049a | Maker-side product read queries: GetMyProducts paged + GetMyProductById; IDOR shield at handler + projection; soft-delete bypass for owner dashboard | 3 | M | **done** | T-0041, T-0043 | US-maker-0004 | — |
| T-0049b | [ProducesResponseType] on Web.Maker ProductController + NSwag regen of maker-api.v1.ts + platform-wide AddMakablesOpenApi enum schema transformer (fixes silent priceType wire-contract bug across every host) | 3 | S | **done** | T-0049a | — | — |
| T-0049c | IOperationFilter for multipart endpoints: rewrite the request body schema so IFormFile lands as { file: binary } with required: true, instead of the current FileParameter optional union or the synthetic Body class that inlines the entire IFormFile interface. T-0049b documented the gap; defensive empty-file check in UploadImage still enforces at runtime, so the client-ergonomic fix is non-blocking. | 3 | S | **in_progress** | T-0049b | — | — |

**Phase 3 total:** 14 tickets (T-0046b promoted to a first-class row during Sprint 6; T-0049a/b/c added during Sprint 6 to bridge backend prep + a deferred multipart-schema fix).

---

## Phase 4 — Orders (placement → payment → escrow → delivery)

| Ticket | Title | Phase | Size | State | Depends on | Stories | ADRs |
|---|---|---|---|---|---|---|---|
| T-0060 | Order entity + state machine + IOrderRepository (scoped ForCustomer / ForMaker / Unscoped) | 4 | L | **done** | T-0033, T-0041 | — | 0002, 0003, 0009, 0011, 0013, 0014, 0016, 0017, 0020 |
| T-0061 | OrderPricing domain service + PricingService orchestrator; adds CountryConfiguration.DefaultShippingPriceMinor; reads CountryConfiguration; tests | 4 | M | **done** | T-0010, T-0041, T-0060 | — | 0003, 0004 |
| T-0062 | OrderNumberGenerator: TZ-aware year (drops `int year` parameter; reads CountryConfiguration.TimeZoneId) + Postgres Testcontainers harness + race/rollback/first-allocation test coverage. Rescoped from "integration into CreateOrder" since that handler is T-0063 | 4 | S | **done** | T-0007, T-0010, T-0060 | — | 0009 |
| T-0063 | CreateOrder command + Validator + Handler + customer-host OrdersController; adds RequireEmailConfirmed middleware; 5 new BusinessErrorMessage codes + Czech i18n; persists Order in `PendingPayment` (Comgate session is a follow-up T-0065 call per user decision Q1) | 4 | L | **done** | T-0060, T-0061, T-0062, T-0033, T-0041, T-0024 | US-customer-0010, 0011 | 0002, 0003, 0005, 0009, 0011, 0013, 0014, 0017 |
| T-0064 | Order attachments — new `OrderAttachment` child entity + migration; multipart upload `POST /api/v1/orders/{id}/attachments` on Web.Customer (PDF/JPEG/PNG/WebP, 10 MiB, max 10/order; allowed states: PendingPayment/Paid/Accepted); streaming download `GET /api/v1/orders/{id}/attachments/{attachmentId}` on BOTH Web.Customer + Web.Maker with ADR 0013 scoping; 3 new BusinessErrorMessage codes + Czech i18n | 4 | M | **done** | T-0042, T-0049a, T-0049c, T-0063 | US-customer-0010; US-maker-0010 | 0011, 0013, 0014 |
| T-0065 | `IPaymentProvider` + `ComgatePaymentProvider` (4-method interface; CreatePayment + VerifyPayment implemented, Webhook + Refund declared as `NotSupportedException` for T-0066/T-0105); `IPaymentProviderFactory` keyed by `CountryConfiguration.DefaultPaymentProvider`; new `POST /api/v1/orders/{id}/payment-session` endpoint on Web.Customer; verify-then-recreate retry policy with `Order.PaymentRedirectUrl` cached for 24h window; 7 new BusinessErrorMessage codes + Czech i18n. Sets the keyed-services-with-factory precedent for future SK/PL/HU provider adapters | 4 | L | **done** | T-0063 | US-customer-0010 | 0002, 0005, 0008, 0014, 0016, 0023 |
| T-0066 | Comgate webhook controller (`POST /api/v1/public/webhooks/comgate`) on Web.Public: three-layer security (`IComgateWebhookIpAllowlist` filter accepting bare IPs + CIDR → re-fetch via `VerifyPaymentAsync` → idempotency check via `GetByPaymentProviderRefAsync`); replaces `ParseAndVerifyWebhookAsync` stub from T-0065; ships **stub `MarkOrderPaid.Command`** doing state transition only (no outbox emission — T-0067 territory); 3 new `payment.webhook.*` BusinessErrorMessage codes + Czech i18n | 4 | M | **done** | T-0065 | US-customer-0010 | 0002, 0005, 0016, 0020 |
| T-0067 | Extend T-0066's `MarkOrderPaid.Handler` with outbox emission (2 events at MVP: customer order-paid email + maker order-placed email; **invoice.generate deferred to T-0068b** per user decision Q2). Ships `AddOrderPaymentMethod` migration + extends `Order.MarkAsPaid` to `(clock, providerRef, paymentMethod, paidAtOverride)` so T-0066's Command fields actually get persisted. Widens `WebhookPayload` with nullable `PaidAt` to trust Comgate's authoritative timestamp. Refactors `EmailSendService.SendAsync` to a per-event-type switch branch (first non-auth email shape). Pre-bakes action URLs into outbox payloads. New `EmailTemplateType.OrderPaidCustomer` + `OrderPlacedMaker` enum values + seed-row migration (cs-CZ + en-US translations) | 4 | M | **done** | T-0066, T-0011, T-0028 | US-customer-0010, US-maker-0006 | 0014, 0016, 0019, 0020 |
| T-0068a | **Invoice entity + IInvoiceRepository + InvoiceNumberGenerator TZ-aware-year migration.** Ships pure-domain + DB slice: sealed Auditable `Invoice` aggregate (Customer XOR Fee, immutable post-issuance), `Invoice.Issue` factory, `Invoice.AttachPdfBlobPath` set-once; `IInvoiceRepository` (ForCustomer/ForMaker/Unscoped per ADR 0013, no Update/Delete); `InvoiceConfiguration` with snake-case + indexes (unique invoice_number, partial unique order_id WHERE is_active, composite maker_id+created_at DESC); EF migration; `IInvoiceNumberGenerator` signature migration (drops `int year` parameter — TZ-aware per CountryConfiguration mirroring T-0062); reuses `PostgresHarness` for race-safety tests. **Locked decisions** (per /feature deliberation): JVM YORE s.r.o. as legal seller, not VAT-registered (`InvoicingMode.None` default), DUZP = `Order.PaidAt`, single shared `FV-CZ-YYYY` sequence. No QuestPDF, no service, no blob — those are T-0068b | 4 | M | **done** | T-0011, T-0042, T-0060, T-0061, T-0062 | US-customer-0010, 0017; US-admin-0012 | 0003, 0009, 0011, 0013, 0014 |
| T-0068b | **IInvoiceService.IssueAsync + QuestPDF renderer + blob upload + MarkOrderPaid InvoiceGenerate enqueue.** `IPdfRenderer` abstraction + new `Infra.PdfRendering` project with `QuestPdfInvoiceRenderer` (Community license pinned, Noto Sans Czech-glyph subset, two `IDocument` templates for `None` Doklad o prodeji + `StandardVat` § 29 daňový doklad); SPAYD QR on every invoice; `InvoiceService.IssueAsync` with `InvoicingMode` switch (None + StandardVat ship; ReverseCharge + StrictFiscalReporting return `InvoicingModeNotImplemented`); blob upload to `invoices/{cc}/orders/{orderId}/{invoiceNumber}.pdf` with deterministic-content idempotency; new `OutboxEventTypes.InvoiceGenerate` + `InvoiceGenerateOutboxPayload`; `MarkOrderPaid.Handler` gains the third `outbox.Enqueue` call deferred by T-0067 | 4 | M | **done** | T-0067, T-0068a | US-customer-0010, 0017; US-admin-0012 | 0003, 0009, 0011, 0013, 0014, 0019, 0020 |
| T-0069 | **GenerateInvoiceFunction + customer email PDF attachment.** Queue-triggered Function dispatches `IssueInvoice.Command` via MediatR (T-0029 SendEmailFunction precedent); new `generate-invoice` queue (separate from email queue for failure isolation); `OutboxDispatcher` branch-routes `invoice.generate` to `PublishGenerateInvoiceAsync`. Extends `IEmailSendService.SendOrderPaidCustomerEmailAsync` with lookup-at-send-time + retry: fetches Invoice via `GetByOrderIdAsync`, downloads PDF via `IBlobStorageClient.DownloadAsync`, attaches via new `Attachment` record on `EmailMessage`. Race handling: invoice not yet rendered → `Transient(InvoiceNotYetRendered)` → outbox retry policy re-delivers. Language-aware filename: `faktura-…pdf` (cs-CZ) / `invoice-…pdf` (en-US). 3 new BusinessErrorMessage codes + Czech i18n. SendGrid 30 MB cap → `Permanent(InvoicePdfAttachmentTooLarge)` + stall | 4 | M | **done** | T-0029, T-0068b | US-customer-0017 | 0019, 0020 |
| T-0070 | **IShippingCarrier + PacketaShippingCarrier + IShippingCarrierFactory + public widget-config endpoint.** Mirrors T-0065 IPaymentProvider precedent: 4-method adapter interface (`Code`, `WidgetConfig` sync, `CreateShipmentAsync`, `GetStatusAsync`, `GetLabelPdfAsync`); keyed-services-with-factory selected per country via `CountryConfiguration.DefaultShippingCarrier = "packeta"` (CZ seed migration). Adds `Order.ShippingCarrierTrackingUrl VARCHAR(500) NULL` column (T-0072 is the writer). New `Web.Public/Controllers/ShippingController` with `GET /api/v1/public/shipping/widget-config?country=CZ&locale=cs-CZ` (anonymous, 100/min per-IP rate limit, `Cache-Control: public, max-age=3600`). `PacketaOptions` (Key Vault: `ApiKey`, `PublicWidgetKey`, `SenderLabel`, `BaseUrl`, `WidgetScriptUrl`, `TestMode=false`) with `ValidateOnStart`. 4 new BusinessErrorMessage codes + Czech i18n. **Locked decisions** (per /feature deliberation): pre-compute + store tracking URL; query-param route; 1-hour long-cache; same 7-day auto-deliver window for personal-pickup as Zásilkovna; new dedicated `ShippingController`; 100/min rate limit; flat `invoices/{cc}/orders/{orderId}/label.pdf` blob path. NSwag regen in same PR | 4 | M | **done** | T-0010 | US-customer-0010; US-maker-0007 | 0016, 0017 |
| T-0071 | **AcceptOrder command (maker action) + customer-notification outbox event.** Maker clicks Accept on Paid order; `Order.Accept(clock)` state transition (Paid → Accepted); single `order.accepted.customerEmail` outbox event with pre-baked ActionUrl (T-0067 precedent). New `EmailTemplateType.OrderAcceptedCustomer` + seed migration (cs-CZ + en-US). New endpoint `POST /api/v1/maker/orders/{orderId}/accept` with IDOR-scoped `GetByIdForMakerAsync`. **Locked decisions:** no DeclineOrder counter-action (admin via T-0107); no auto-cancel deadline (maker SLA + dashboard nudges T-0087). NSwag regen | 4 | S | **done** | T-0060, T-0011 | US-maker-0006 | 0013, 0014, 0019, 0020 |
| T-0072 | **ShipOrder command (Zásilkovna path) + atomic 2-event outbox.** Maker ships Zásilkovna order; calls `IShippingCarrierFactory.ResolveAsync` → `carrier.CreateShipmentAsync`; `Order.Ship(clock, carrierRef, 7d, trackingUrl)` (signature extended in-place with 4th param); atomic enqueue of `order.shipped.customerEmail` + `shipping.generate.label` (T-0067 atomic-events pattern). `OutboxDispatcher` new branch for `shipping.generate.label` → new `generate-label` Storage Queue + `PublishGenerateLabelAsync`. `OrderShippedCustomerEmailPayload` with optional `TrackingUrl` (T-0073 PersonalPickup passes null; SendGrid template conditionally renders). `EmailTemplateType.OrderShippedCustomer` + cs-CZ + en-US seed. New `ShippingMethodNotEligible` BusinessErrorMessage. Endpoint `POST /api/v1/maker/orders/{orderId}/ship`. NSwag regen | 4 | M | **done** | T-0070, T-0071 | US-maker-0007 | 0014, 0017, 0019, 0020 |
| T-0073 | **HandOverOrder command (personal-pickup path) + unified customer-shipped email.** Maker clicks 'Předáno' on PersonalPickup order; `Order.Ship(clock, null, 7d, null)` (no carrier ref, no tracking URL, same 7-day auto-deliver window as Zásilkovna per T-0070 lock). Single `order.shipped.customerEmail` outbox event REUSED from T-0072 (template conditionally renders tracking row). New endpoint `POST /api/v1/maker/orders/{orderId}/handover`. **Locked decisions:** unified email template with T-0072 (single SendGrid Dynamic Template); self-attested handover (no photo/signature capture; admin dispute path via T-0079 messages + T-0118 monitoring). NSwag regen | 4 | S | **done** | T-0071 | US-maker-0008 | 0014, 0017, 0019, 0020 |
| T-0074 | **GenerateLabelFunction (queue-triggered) + FetchAndStoreShippingLabel command.** Thin MediatR dispatch wrapper (T-0069 GenerateInvoiceFunction precedent). Consumes `shipping.generate.label` queue → `FetchAndStoreShippingLabel.Command(OrderId)` → loads Order via `GetByIdUnscopedAsync` → HEAD-check blob at `invoices/{cc}/orders/{orderId}/label.pdf` (idempotent pre-check; saves Packeta quota on retries) → if missing, `carrier.GetLabelPdfAsync` → upload to blob. Silent surface (proactive background job; ops investigates outbox stalls). No Order state mutation (blob existence is the only state). NO controller (Function-only). Reuses ShippingCarrier* BusinessErrorMessage codes from T-0070 | 4 | M | **done** | T-0042, T-0070, T-0072 | US-maker-0009 | 0014, 0017, 0020 |
| T-0075 | **Label download endpoint (maker) — cache → Packeta fallback.** `GET /api/v1/maker/files/orders/{orderId}/label` on Web.Maker. New `FilesController` with `[Authorize]` + maker scoping via `GetByIdForMakerAsync`. Cache-hit: stream from `invoices/{cc}/orders/{orderId}/label.pdf` with `Cache-Control: public, max-age=31536000, immutable`. Cache-miss: fire-and-forget `Task.Run` triggers background blob upload while streaming live Packeta response to maker (~1-2s Packeta latency acceptable for ~5-10KB labels). Packeta 5xx → `503 ServiceUnavailable + Retry-After: 60` (correct HTTP semantic). **Locked decisions:** fire-and-forget cache-fill; 503+Retry-After on Packeta blip; 1-year immutable cache on success. Reuses OrderNotFound + ShippingCarrierUnavailable codes | 4 | S | **done** | T-0074 | US-maker-0009 | 0013, 0014, 0017 |
| T-0076 | **MarkOrderDelivered command + customer-notification outbox + DeliverySource tracking.** Single one-file command shared by 3 callers (customer endpoint `POST /api/v1/customer/orders/{id}/deliver`, T-0077 auto-deliver timer, T-0078 carrier sync). New `OrderDeliverySource` enum (Customer=0, Auto=1, Carrier=2) + `Order.delivery_source SMALLINT NULL` column. `Order.MarkAsDelivered(IClock, OrderDeliverySource, DateTimeOffset?)` extended in-place (deliveredAtOverride fallback to clock.UtcNow). Single `order.delivered.customerEmail` outbox event + new `EmailTemplateType.OrderDeliveredCustomer` template (cs-CZ + en-US seed). **Locked decisions:** DeliverySource as column (not payload-only); customer email only (no maker — T-0102 payout cron is the contract); silent Success on already-Delivered re-call (mirrors T-0067/T-0069 idempotency); `/deliver` short-verb route. `MarkOrderDeliveredResponse` globally-unique to avoid NSwag TS collision. NSwag regen | 4 | S | **done** | T-0072, T-0073 | US-customer-0013 | 0013, 0014, 0019, 0020 |
| T-0077 | **AutoDeliverOrdersFunction (timer daily 08:00 UTC).** Thin MediatR-dispatch wrapper (T-0029 ProcessOutboxFunction precedent). Reads `IOrderRepository.GetAutoDeliverableUnscopedReadOnlyAsync(asOf, ct) → IAsyncEnumerable<string>` (OrderId-only projection + AsNoTracking per shipping-bundle Gate 8 fold). For each: dispatch `MarkOrderDelivered.Command(OrderId, OrderDeliverySource.Auto)`. **Locked PM-absorbed:** unlimited batch at MVP; stateless re-fetch on partial-run failure; fail-continue per-Order (one bad row doesn't block batch); silent-Success race handled by T-0076 contract. Zero new error codes, i18n, outbox events, controllers, schema | 4 | S | **done** | T-0076 | US-customer-0013 | 0014, 0017, 0020 |
| T-0078 | **SyncShipmentStatusesFunction (timer every 6h) + DisputeShipment stub.** Thin Function (~50 lines) calls `IShippingCarrierFactory.ResolveAsync(countryCode) → carrier.GetStatusAsync(carrierRef)` per Shipped Zásilkovna order. Branch on returned `ShipmentState`: Delivered → dispatch `MarkOrderDelivered.Command(OrderId, Source: Carrier, packetaDeliveredAt)`; Returned/Failed → dispatch new `DisputeShipment.Command` stub (T-0106 will wire real domain logic). New `DisputeReason` enum + `OrderDisputedCarrierSourced` outbox event + payload. **Locked decisions:** stub DisputeShipment.Command (T-0078 owns detection, T-0106 owns response); DeliveredAt fallback to clock.UtcNow + log Warning when Packeta omits timestamp. Repository predicate excludes PersonalPickup. Polly resilience on carrier calls. Fail-continue per Order | 4 | M | **done** | T-0070, T-0076 | US-customer-0013 | 0014, 0017, 0020 |
| T-0079 | **OrderMessage thread (customer ↔ maker).** New `OrderMessage : Auditable` entity + EF migration adding `orders.customer_unread_message_count` + `orders.maker_unread_message_count` (denormalized; closes T-0081's UnreadMessageCount forward-compat null). 6 one-file features (PostMessage + GetMessages + MarkAsRead × 2 audiences) per compile-time IDOR shield. POST/GET `/api/v1/{audience}/orders/{id}/messages` + POST `.../mark-read`. **Locked decisions:** two-party only (admin read-only via T-0111); 5-min digest outbox (`order.message.posted.{customer,maker}Email`); UnreadMessageCount denormalized on Order; 2000-char body cap. New BusinessErrorMessage codes: `OrderMessageBodyEmpty`, `OrderMessageBodyTooLong`. NSwag regen customer + maker hosts | 4 | M | **done** | T-0060, T-0011, T-0081 | US-customer-0014; US-maker-0011 | 0013, 0014, 0017, 0019, 0020, 0023 |
| T-0080 | **GetCustomerOrders paged + filtered list query.** `GET /api/v1/customer/orders?page=1&pageSize=20&state=...&dateFrom=...&dateTo=...&sort=...`. New `IOrderQueries` interface (CRUD/Queries split per T-0049a). New `OrderSort` enum + `OrderFilter` record. Flat `CustomerOrderListItemDto` with denormalized MakerName + first ProductTitle (nullable for custom orders). AsNoTracking projection-only EF read. `WHERE CustomerUserId == sessionCustomerId` predicate IS the IDOR shield. PageSize clamp 1-50. Globally-unique `GetCustomerOrdersResponse`. **Locked decisions:** page-based pagination (bundle-wide); State+DateRange filters only (no text search at MVP); GET with query params; flat DTO with denormalized maker/product. NSwag regen customer host | 4 | M | **done** | T-0060 | US-customer-0016 | 0013, 0014, 0023 |
| T-0081 | **GetMakerOrders paged + filtered list query.** `GET /api/v1/maker/orders` with same shape as T-0080. New `MakerOrderListItemDto` (different fields: MakerPayoutAmountMinor instead of PlatformFee; CustomerContactName but NOT email per GDPR; UnreadMessageCount nullable forward-compat for T-0079). Extends `IOrderQueries` with `GetMakerOrdersPagedAsync`. **Locked decisions:** mirror T-0080's 4 bundle-wide locks; customer email NEVER exposed (GDPR data minimization; makers use T-0079 messages instead); no backend 'needs action' pseudo-state (frontend builds multi-state query). NSwag regen maker host | 4 | M | **done** | T-0060, T-0080 | US-maker-0005 | 0013, 0014, 0023 |
| T-0082 | **GetCustomerOrderDetails + GetMakerOrderDetails (audience-specific DTOs).** Two SEPARATE queries (not one shared `audience` parameter) — compile-time IDOR shield via separate `GetByIdForCustomerAsync` / `GetByIdForMakerAsync` paths. Customer DTO: lifecycle timestamps + breakdown + MakerName + ShippingCarrierTrackingUrl. Maker DTO: lifecycle + MakerPayoutAmountMinor + CustomerContactName/Phone (no email) + ShippingCarrierRef. Both include inline `Attachments[]` (T-0064 bounded at 10) + inline `InvoicePdfUrl: string | null` (backend-built URL). `GET /api/v1/customer/orders/{id}` + `GET /api/v1/maker/orders/{id}`. **Locked decisions:** two separate queries (cleaner DI + compile-time IDOR); inline attachments (saves a roundtrip per render); inline InvoicePdfUrl (backend owns URL construction). NSwag regen both hosts | 4 | M | **done** | T-0060, T-0080, T-0081 | US-customer-0012; US-maker-0010 | 0013, 0014, 0023 |
| T-0083 | **CancelExpiredPendingPaymentOrdersFunction (timer, daily 02:00 UTC).** Thin Function (~50 lines) mirroring T-0077 AutoDeliver: dispatches `CancelExpiredOrder.Command(orderId)` per Order with `State==PendingPayment AND CreatedAt < asOf - 24h`. New one-file feature owns the state transition + outbox emit. New `OrderCancellationSource` enum (Customer, AutoExpiry, Admin). New OutboxEventType `OrderCancelledCustomerEmail` + payload + EmailSendService routing branch + cs-CZ template. **Locked decisions:** 24h TTL (matches Comgate retry window); customer email only (maker never knew); MediatR-dispatch wrapper (per ADR 0020); daily 02:00 UTC schedule (off-peak from AutoDeliver). Silent Success on already-Cancelled race. Q-0008 MARS workaround (materialize List<string> before per-row mediator.Send) | 4 | S | **done** | T-0066, T-0067 | US-customer-0010 | 0017, 0019, 0020, 0023 |
| T-0084a | **Checkout order form `/objednavka?productId=`** (L-split a of T-0084, bundle 1 `feat/checkout-flow-bundle` pos 1). Single page + sticky order summary (Q1 lock); attachments picked in-form, held client-side, auto-uploaded post-create, partial failures land on T-0084b retry surface (Q2 lock); T-0063 validator UX mirrors (Czech phone, note ≤2000); Packeta widget modal via official script + widget-config endpoint, failure → disable option; PersonalPickup gating; disabled-button idempotency; `orders-client.ts` helper. ~30 `checkout.*` i18n keys | 4 | M | **ready** | T-0063, T-0064, T-0065, T-0070 | US-customer-0010, 0011 | 0022, 0024 |
| T-0084b | **Pre-payment order page `/objednavka/[id]`** (L-split b, bundle 1 pos 2). PendingPayment-only surface: explicit "Zaplatit" button creates payment session ON CLICK — never on page load (Q3 lock; network-panel AC pin); attachment retry manager (add up to 10, per-file retry); T-0083 24h auto-cancel notice; `payments-client.ts`; non-PendingPayment states show minimal banner until T-0086b (loudly incomplete per no-mocks) | 4 | M | **ready** | T-0084a | US-customer-0010 | 0022, 0024 |
| T-0085 | **Payment confirmation `/objednavka/[id]/potvrzeni`** (bundle 1 pos 3, Comgate returnUrl target). Optimistic render from ?status= + 3s poll of detail endpoint until State==Paid, ~30s cap → "verifying" + detail link (Q4 lock; webhook-is-truth per CLAUDE.md — crafted-param attack AC included); cancelled/failed → retry CTA into T-0065 24h window; visibility-pause. Manual step: Comgate merchant-portal return URLs per env | 4 | S | **ready** | T-0084b | US-customer-0010 | 0022, 0024 |
| T-0086a | **Customer order list `/dashboard/zakaznik/objednavky`** (L-split a of T-0086, bundle 2 `feat/order-dashboards-bundle` pos 3). SSR + URL-state filters/sort/pagination (Q5 lock, no client store); unread badges from T-0089 projection (Q7 lock); state+date filters per T-0080 GET params; 5-arm sort; katalog-precedent pagination; empty state → /katalog CTA; mobile cards / desktop table | 4 | M | **ready** | T-0089, T-0080 | US-customer-0016 | 0022, 0024 |
| T-0086b | **Customer order tracking `/objednavka/[id]` post-payment states** (L-split b, bundle 2 pos 4). Creates the SHARED `OrderMessageThread` component (inline at detail bottom per Q6 lock; mark-read on render; ~30s visibility-paused poll — the platform's ONLY polling surface per Q5): lifecycle timeline from nullable timestamps; price breakdown; tracking link; Shipped-only confirm-delivery → T-0076 + router.refresh; attachments; invoice link only when InvoicePdfUrl non-null (T-0088); PendingPayment post-box guard per T-0079 ruling | 4 | M | **ready** | T-0088, T-0082, T-0079, T-0076, T-0084b | US-customer-0012, 0013, 0014 | 0022, 0024 |
| T-0087a | **Maker order list `/dashboard/maker/objednavky`** (L-split a of T-0087, bundle 2 pos 5). State tabs Nové(Paid)/Ve výrobě(Accepted)/Vše — one single-state request per tab, default Nové as the T-0071-deferred needs-action nudge, tab in URL param (Q8 lock); unread badges (real since T-0079); payout column (MakerPayoutAmountMinor, never platform fee); CustomerContactName never email (GDPR); per-tab empty states | 4 | M | **ready** | T-0081, T-0079 | US-maker-0005, 0006 | 0022, 0024 |
| T-0087b | **Maker order detail + actions `/dashboard/maker/objednavky/[orderId]`** (L-split b, bundle 2 pos 6). State-aware buttons rendered from State+ShippingMethod: Paid→Přijmout (T-0071), Accepted→Odeslat (T-0072, confirm dialog — irreversible carrier shipment) / Předat osobně (T-0073, PersonalPickup only); POST + router.refresh (Q5); REUSES T-0086b's OrderMessageThread with maker trio (Q6); label download via blob-helper (NSwag file-response gap — generated `label()` discards body); contact card name+phone NO email; adds missing `order.notFound` + `order.invalidTransition` i18n parity keys | 4 | M | **ready** | T-0071, T-0072, T-0073, T-0075, T-0082, T-0086b, T-0088 | US-maker-0007, 0008, 0010, 0011 | 0022, 0024 |
| T-0088 | **Invoice download endpoints (customer + maker)** (backend slice, bundle 2 pos 1). Makes T-0082's InvoicePdfUrl placeholder real: `GET /api/v1/orders/{orderId}/invoice` on BOTH hosts (host-relative routes exactly as T-0082's projection emits); controller-direct streaming per T-0075 FilesController precedent (no MediatR feature); ADR 0013 ownership scoping; 404 when no Invoice or PdfBlobPath null; reuses OrderNotFound + InvoiceNotYetRendered (zero new codes); `private, no-store` + ETag (T-0064 PII policy); adds `IOrderRepository.GetByIdForCustomerReadOnlyAsync`; ~6 unit + 2 integration tests; NSwag regen both hosts | 4 | S | **ready** | T-0068b, T-0069, T-0082 | US-customer-0017 | 0013, 0023, 0025 |
| T-0089 | **Customer UnreadMessageCount wire pin** (backend slice, bundle 2 pos 2 — thin verification gate). Core gap ALREADY SHIPPED by PR #44 fold (DTO field + projection + NSwag regen all on master, PM re-verified). Residue: 1 missing customer-side integration test mirroring the maker `GET_orders_UnreadMessageCount_returns_denormalized_value` pin. Test-only diff; keeps the bundle's regen-gate slot explicit | 4 | S | **ready** | T-0079, T-0080 | US-customer-0016 | 0013, 0023 |

**Phase 4 total:** 33 tickets (T-0068 split a/b; T-0084/T-0086/T-0087 L-split a/b each; +T-0088/T-0089 backend gap slices discovered in the 2026-06-09 frontend grooming). The bulkiest phase.

---

## Phase 5 — Post-order (reviews, payouts, admin operations)

| Ticket | Title | Phase | Size | State | Depends on | Stories | ADRs |
|---|---|---|---|---|---|---|---|
| T-0100 | **Review entity + SubmitReview / RespondToReview (per-delivered-order grain)** (bundle `feat/reviews-loop-bundle` pos 1). New `Review : Auditable` aggregate + `IReviewRepository` (write-side, ADR 0013-scoped) + `IReviewQueries` (read-side, 3 dashboard reads) + EF migration (**partial unique index `(order_id) WHERE is_active`** + `(maker_id, created_on DESC)`); per-audience compile-time command split — `SubmitReview` on Web.Customer only, `RespondToReview` on Web.Maker only (cross-tenant → 404, no existence leak); `Maker.RecomputeRating` wires the dormant T-0043 `RatingAverageBp`/`RatingCount` via recompute-from-rows in the same UoW. 5 new BusinessErrorMessage codes + cs-CZ. **Locked decisions** (per 2026-06-14 deliberation): Q1 per-delivered-order grain (one review/order, unique partial index is the anti-abuse anchor); Q2 rating 1–5 required, body optional ≤1000; Q3 eligibility = any Delivered/Completed order, no time limit; Q4 customer review immutable, maker reply overwritable one-per-review ≤500; Q5 `rating_avg` live from first review (recompute-from-rows, self-healing under soft-delete). Public review LIST carved out to T-0050. NSwag regen BOTH customer + maker hosts | 5 | M | **ready** | T-0043, T-0060, T-0076 | US-customer-0015; US-maker-0014 | 0013, 0014, 0023 |
| T-0050 | **GetMakerReviews public query + T-0047 profile review-list binding** (deferred carve-out from `feat/reviews-loop-bundle` — public review LIST). Populates the forward-compat empty `MakerProfile.Reviews` placeholder (`ICatalogQueries.cs:100`) + `MakerReviewItem` DTO (`:115`, names T-0050 as producer): public-host read of the latest N (5 per US-customer-0008 AC-3) **active** reviews newest-first w/ maker reply, lean default = fold inline into `GetMakerBySlugAsync` (no new endpoint); binds the T-0047 reviews-section body (replaces the empty-state). The aggregate star NUMBERS are already public via T-0043 — only the per-review list is deferred here. `MakerReviewItem` reply-field addition is a contract change → public-host NSwag regen. **Stays draft / NOT ready:** blocked on T-0100 (review data producer) + open sub-questions (DTO reply shape flat-vs-nested; inline-vs-separate query; Q5 single-review-skew display tweak) need grooming before AC are written G/W/T | 5 | S | draft | T-0100 | US-customer-0008 | 0013, 0022 |
| T-0101 | **PayoutBatch entity + Order.PayoutBatchId + IPayoutBatchRepository + payout_batches migration** (bundle `feat/payout-core-bundle` pos 1, T-0068a entity-slice precedent). Born-`Processing` immutable batch (Q4 lock — no `Pending`); `AttachCsvBlobPath` set-once mirroring `Invoice.AttachPdfBlobPath`; `Order.AssignToPayoutBatch` set-once; unique `(country_code, batch_number)` enforces ADR 0009 numbering + partial unique open-batch-per-country index (timer/admin race guard); closes the T-0068a `invoices.payout_batch_id` FK TODO; Q-0017 data-fix migration (16 single-brace email subject rows) leads the bundle PR. No commands, no NSwag | 5 | M | **ready** | T-0007, T-0060, T-0068a | US-admin-0007 | 0009, 0011, 0013, 0014 |
| T-0102a | **CreatePayoutBatch command — claim Delivered orders into an immutable weekly batch** (L-split a of T-0102, bundle pos 2). Admin-host `POST /api/v1/payout-batches`; pure `PayoutEligibility.Classify` spec is the red-first TDD surface; partially-refunded orders excluded (Q3 lock); NULL-`BankAccount` makers excluded (Q5 lock); re-run returns the open batch (`AlreadyExisted = true`, US-admin-0007 AC-4); week guard `payoutBatch.weekAlreadyProcessed`; TZ-aware ISO-week `VYP-CZ-YYYY-Www` derivation in handler; handler-written audit row (real batch id + run-summary afterJson); first `MakablesMeters.Payouts` instruments; currency-homogeneity guard; 2 new codes + 3 cs-CZ keys. NSwag admin regen in the bundle's final regen commit | 5 | M | **ready** | T-0101, T-0105 | US-admin-0007 | 0009, 0013, 0014, 0023 |
| T-0102b | **Payout batch artifacts — per-maker Fee invoices + bank CSV + fee-invoice maker emails + admin CSV download** (L-split b of T-0102, bundle pos 3, same PR as T-0102a). Generic documented CSV behind new `IPayoutCsvFormatter` seam (Q1 lock — bank-native exporters are follow-ups once the operator names the bank); one `InvoiceType.Fee` invoice per maker per batch, DUZP = batch creation date, shared FV-CZ sequence (Q2 lock); first user of `Invoice.PayoutBatchId` XOR side; `ProvizniDokladDocument` QuestPDF template; `payout.feeInvoice.makerEmail` outbox with T-0069 lookup-at-send attachment pattern; resumable artifact re-entrancy (claim commits even on artifact failure; re-run completes missing artifacts); `GET /api/v1/admin/payout-batches/{id}/csv` controller-direct streaming per T-0088 precedent; frozen §C.5 CSV golden-file spec; 3 new codes + cs-CZ. NSwag admin regen same PR | 5 | M | **ready** | T-0101, T-0102a, T-0068b, T-0069 | US-admin-0007; US-maker-0012 | 0003, 0009, 0019, 0020 |
| T-0103 | **MarkPayoutBatchCompleted command — settle a Processing batch (orders Delivered → Completed, per-maker payout-sent emails)** (bundle `feat/payout-settlement-bundle` pos 1, PR #2 of the payout chain). Admin-host `POST /api/v1/payout-batches/{id}/complete`; captures `BankReference` (required) + `PaymentDate` (`DateOnly?`, optional → `CompletedAt = clock.UtcNow` when omitted, else midnight-UTC of the value date) — Q1 lock. New `PayoutBatch.Complete(clock, bankRef, paymentDate, completedBy)` domain seam (ships with its only caller) + `BankReference` column migration; materialized `Order.Complete(clock)` loop in ONE handler/UoW, **no per-order `mediator.Send`** (Q2 lock — Q-0008 MARS guard); one `payout.payoutSent.makerEmail` outbox event **per distinct maker** with summed `MakerTotalPaidMinor` + dedup (Q3 lock); `PayoutSentMaker` template seed (double-brace subjects per Q-0017) + `EmailSendService` branch. Silent-Success on already-`Completed` re-call; **no un-complete — completion is financially terminal, errors corrected forward via T-0105/T-0107** (Reversibility lock). `IAdminAuditableCommand` audit (real batch id); reuse `PayoutBatchNotFound`, one new `PayoutBatchNotProcessing` + cs-CZ. Admin NSwag regen deferred to T-0118 (no admin client consumer yet) | 5 | M | **ready** | T-0102a, T-0102b, T-0011 | US-admin-0007; US-maker-0012 | 0009, 0014, 0019, 0020 |
| T-0104 | **RunWeeklyPayoutBatch Function (timer Monday 02:00 UTC + HTTP escape hatch)** (bundle pos 4, scheduler). Thin ADR 0020 wrapper per T-0029/T-0077 precedents: dual trigger (`%RunWeeklyPayoutBatch:Schedule%` default `0 0 2 * * 1`, `UseMonitor = true`; `POST /api/payouts/run-batch` at `AuthorizationLevel.Function`); dispatches parameterless `CreatePayoutBatch.Command` exactly once; four-branch response interpretation (created / `AlreadyExisted` / `payoutBatch.empty` = Information quiet week / other = Error); no per-row loop (no MARS concern); zero business logic, zero new endpoints on Web.* hosts, no NSwag | 5 | S | **ready** | T-0102a, T-0102b | US-admin-0007 | 0020 |
| T-0105 | **RefundOrder command (admin) + Comgate RefundAsync + order-refunded email** (bundle `feat/refund-dispute-bundle` pos 1). Full + partial refunds (Q1 lock): amount ≤ remaining accumulates new `Order.RefundedAmountMinor`; state flips to Refunded only when cumulative == total; no credit-note at MVP. Provider-first order of operations: `RefundAsync` BEFORE mutation. Completed-state refund needs `AcknowledgePostPayout` (Q5 lock); only sanctioned path into Refunded (Q4 interlock). FIRST admin-host controller + first `admin-api.v1.ts` NSwag regen; 4 `payment.refund.*` codes; `order.refunded.customerEmail` outbox; red-first domain predicates | 5 | M | **ready** | T-0066, T-0067 | US-admin-0008 | 0014, 0016 |
| T-0106 | **OpenDispute + ResolveDispute — Dispute child entity, 4-source open, auto-deliver exclusion** (bundle pos 2). State + child entity (Q2 lock): `Disputed` parenthesis state with `Order.PreDisputeState` restore (patterns A.22); `Dispute : Auditable` (Category/Source/Description/ResolutionNotes/Outcome). Open endpoints for all 4 sources NOW, UI later (Q3 lock); rewires T-0078 carrier stub; disputable states Paid–Delivered (Completed out). Resolve restores state then dispatches sanctioned commands — Refunded outcome → full-remaining `RefundOrder` (Q1). T-0079 thread stays open as evidence channel. Manual step: `ADMIN_NOTIFICATION_EMAIL` env var. NSwag regen 3 hosts | 5 | M | **ready** | T-0011, T-0060, T-0077, T-0078, T-0079, T-0105 | US-admin-0011 | 0011, 0013, 0014, 0017, 0020 |
| T-0107 | **ChangeOrderStateManually (admin escape hatch, strict allow-list, required reason)** (bundle pos 3). Q4 lock: explicit transition table — never → Paid without providerRef, never → Refunded (use RefundOrder T-0105), never into/out of Disputed (use T-0106), never out of Refunded; blocked transitions return codes naming the sanctioned command; mandatory reason (10–2000 chars) → audit Notes. Pure `ManualOrderTransitionPolicy` predicate is THE red-first TDD surface (table-driven full state-matrix tests). New `Order.RevertAcceptance` + `OrderDeliverySource.AdminManual`; 6 `order.manualTransition.*` codes; no migration; admin-host NSwag regen | 5 | M | **ready** | T-0060, T-0083, T-0105, T-0106 | US-admin-0010 | 0014 |
| T-0108 | **UpdateCountryConfiguration admin command (VAT / fee / providers / invoicing mode)** (bundle `feat/admin-ops-bundle` pos 3 — the control-plane mutation). One-file feature wrapping the existing `CountryConfiguration` mutators; `IAdminAuditableCommand` before/after JSONB + UoW (handler never `SaveChangesAsync`). **Locked (2026-06-14 deliberation, Q-C):** provider change requires retyping the new provider code (`ConfirmedProviderCode` == new value, else `country.providerConfirmationMismatch`); unregistered provider codes rejected at write time against the DI registry via the new `IProviderRegistry` domain seam (else `country.providerNotRegistered`); in-flight orders keep their cached `PaymentProviderRef` — WARN via advisory `Response.InFlightOrderCount`, never block. **Q-0021:** no-op all-values-unchanged → Silent-Success 200, no mutators, but the audit pipeline still writes the benign "admin attempted country.update" row (accepted; the dropped "no second audit row" wording is gone platform-wide). No migration (entity + columns on master); 1 new code + 3 cs-CZ keys; `CountInFlightByCountryAsync` read method; admin-host NSwag regen; security_touching | 5 | M | **ready** | T-0105 | US-admin-0006 | 0014 |
| T-0109 | **RetryOutboxEvent + AcknowledgeOutboxEvent admin commands** (bundle `feat/admin-ops-bundle` pos 2 — the smallest mutation). Two one-file features nudging a single stalled `OutboxEvent`; both `IAdminAuditableCommand` + fail-closed session; no migration, no outbox emission, no email. One new domain method `OutboxEvent.RequeueForRetry(now)`. **Locked (2026-06-14 deliberation, Q-D):** retry = one-shot "try now" — `NextRetryAt = now`, `RetryCount++`, **backoff ladder PRESERVED** (not reset to 0); acknowledge = terminal (the existing `Acknowledge` sets `ProcessedAt` + acks, leaves the stalled set permanently); retry-on-already-processed → clean `outbox.alreadyProcessed` **409** (operator error surfaced loudly); acknowledge-on-already-acknowledged → **Silent-Success** preserving the original acknowledger. Reuse `OutboxRowNotFound` (no `eventNotFound` mint) + reuse `IOutboxConsumerRepository.GetByIdAsync` (no new admin repo); 1 new code (`OutboxAlreadyProcessed`) + 2 cs-CZ parity keys; ~11 unit (red-first on `RequeueForRetry`) + 2 integration; admin-host NSwag regen; security_touching | 5 | S | **ready** | T-0029, T-0103, T-0105 | US-admin-0014 | 0014, 0020 |
| T-0110 | **DeleteUserPermanently command — GDPR hard-delete + PII erasure matrix (the ONLY hard-delete in the system)** (bundle `feat/admin-ops-bundle` pos 4 — built last, risk-ascending). Thin handler (load → 2 gates → invoke seam → return) over the architect-designed `IUserDataDeletionService.EraseAsync(userId)` seam (extension-points §14 / patterns §A.23 — single orchestration, ONE UoW, never `SaveChangesAsync`; the only `Remove()`-on-`User` path, Reviewer-enforced). **Erasure matrix (Q-A, user-locked 2026-06-14):** HARD-DELETE `User` + `RefreshToken` + unreferenced `Address`; ANONYMIZE `Order` contact snapshots + `Review` author + `Maker` PII (RETAIN IČO + `BankAccount`, set new `IsRetainedForLegal = true`); RETAIN `Invoice` rows UNTOUCHED (GDPR Art. 17(3)(b)). **Two gates (Q-B):** admin retypes the user email (`user.deleteConfirmationMismatch`); in-flight-order interlock REJECTS with `user.cannotDeleteWithInFlightOrders` when any order is `[PendingPayment, Paid, Accepted, Shipped]`. **NO Silent-Success on re-call** (the one bundle command without it — re-call → `user.notFound`). `AddMakerIsRetainedForLegal` migration; 2 new codes + reuse the in-flight code; ~12 unit (red-first transforms) + 5 integration; admin-host NSwag regen; security_touching. **§Architect-review: YES — security review + architect sign-off both required on the PR.** | 5 | M | **ready** | T-0033, T-0060, T-0111, T-0109, T-0108 | US-admin-0016 | 0013, 0014 |
| T-0111 | **Admin unscoped paged read queries — orders, invoices, audit log** (bundle `feat/admin-ops-bundle` pos 1 — the read-only verification harness the three mutations assert against). New `IAdminQueries` read-side seam (ADR 0023, mirrors T-0080 `IOrderQueries` split) + 3 filter records + 3 list-item DTOs + 3 one-file query features + `AdminQueries` EF impl + admin controller (3 GET endpoints). All three `Unscoped()` from `Web.Admin` ONLY (ADR 0013 escape hatch); admin-audience JWT is the boundary (security_touching). **Locked (2026-06-14 deliberation, Q-E):** filter set = exactly the AC-named dimensions, no more (orders: state/country/maker/customerEmail; invoices: type/country/recipient/date; audit: adminUser/actionCode/targetEntity/date). Privileged admin order DTO carries `CustomerEmail` + `MakerName` (no GDPR redaction — story-locked divergence from the maker T-0081 surface). `.IgnoreQueryFilters()` ONLY on admin-orders (commented — surfaces T-0110's anonymised rows); no audit row for reads (ADR 0014 audits writes only). No new codes, migrations, outbox, email, or i18n; page clamp [1,50], `CreatedAt DESC`; ~10 unit + 4 integration; admin-host NSwag regen | 5 | M | **ready** | T-0080, T-0105 | US-admin-0009, 0012, 0015 | 0013, 0023 |
| T-0112 | **Maker payout read queries — paged list + per-order detail + order outbox-events** (bundle `feat/payout-settlement-bundle` pos 2). New `IPayoutQueries` (ADR 0023 read-side seam) with `GetMakerPayoutsPagedAsync` / `GetMakerPayoutDetailAsync` / `GetMakerOutboxEventsForOrderAsync` on the `Web.Maker` host. Per-maker slice = `SUM(this maker's MakerPayoutAmountMinor)`, **never** the cross-maker `PayoutBatch.TotalAmountMinor`; **CSV never surfaced to makers** (operator bank file = cross-maker PII, Q4); list = pagination only, sort `CompletedAt DESC, BatchNumber DESC`, no filters (Q5). IDOR shield enforced twice (handler resolves `makerId` from session + projection re-filters), `AsNoTracking` + `IgnoreAutoIncludes`, globally-unique response names (T-0081 verbatim). Outbox-events query is read-only + payload-free (event type + derived `OutboxDeliveryStatus` + timestamp only; no `PayloadJson`/`LastErrorCode`/retry — admin-only retry per US-maker-0017). Exposes `FeeInvoiceId`; the stream is T-0112a. No new codes/migrations/outbox/i18n. Maker NSwag regen (the gate T-0116 + T-0112a consume) | 5 | M | **ready** | T-0101, T-0102a | US-maker-0012, 0013, 0017 | 0013, 0023 |
| T-0112a | **Maker fee-invoice PDF download endpoint** `GET /api/v1/maker/files/invoices/{invoiceId}` (US-maker-0013 gap — T-0088's `GetByOrderId*` chain only ever returns Customer invoices; Fee invoices are unreachable by a maker). Controller-direct stream on the existing T-0075 `FilesController` per T-0088 precedent; new `AsNoTracking` `IInvoiceRepository.GetForMakerReadOnlyAsync` (read-only mirror, ADR 0025) — `MakerId` predicate IS the IDOR shield (denormalised, covers the Fee family); `InvoiceType.Fee` controller gate → `order.notFound` on a Customer-invoice id (single-purpose route, no existence oracle). `private, no-store` + ETag/304 (T-0064 PII policy, not the label's `immutable`). Reuse `OrderNotFound` + `InvoiceNotYetRendered` — no new codes/migrations/outbox/i18n; `ForMaker` `:57` list TODO left standing (T-0112/T-0116 territory). Maker NSwag regen **rides T-0112's regen commit** (same bundle PR) | 5 | S | **ready** | T-0068b, T-0102b | US-maker-0013 | 0013, 0023, 0025 |
| T-0113 | EvictExpiredRegistryCache Function (timer daily 02:00 UTC) | 5 | S | draft | T-0032 | — | 0020 |
| T-0114 | DataRetentionCleanup Function (timer weekly Sunday 03:00 UTC) — placeholder; full GDPR retention policy in v1.1 | 5 | S | draft | T-0011 | — | 0023 |
| T-0115 | **Customer review-submission UI — inline CTA + form on the order-detail page** (bundle `feat/reviews-loop-bundle`, frontend cap). Adds one conditional inline block to the shipped `/objednavka/[id]` page (T-0086b) below the message thread: three render states from backend signals — `canReview` → interactive `ReviewFormClient` island (new hand-built `StarRating` primitive, 1–5 required, optional ≤1000-char comment w/ live counter); existing `review` → read-only `SubmittedReview` block (stars + comment + Czech date + maker-reply panel if present); else nothing. New `reviews-client.ts` submit helper, `customer.review.*` i18n (vykání) + 5 error parity keys. **Locked:** Q1 per-order grain, Q2 stars-required/comment-optional, Q4 customer review immutable (no edit/delete affordance — read-only forever after submit), `router.refresh()` re-sync (no optimistic state). Pure presentation — no backend change, no NSwag regen (consumes the T-0100 contract) | 5 | S | **ready** | T-0100, T-0086b, T-0043 | US-customer-0015 | 0022, 0024 |
| T-0116 | **Maker payout dashboard `/dashboard/maker/vyplaty`** (frontend cap of `feat/payout-settlement-bundle`). Server-Component list + drill-into-batch detail mirroring the `objednavky` maker dashboard (SSR, `force-dynamic`, URL-state `?page` pagination, mobile-cards/desktop-table, `formatCzk`, tykání). List rows: batch number, total paid to this maker, order count, two-value state badge (`Processing → Připravujeme`, `Completed → Vyplaceno` — no `Pending`; supersedes US-maker-0012 AC-2's stale enum), date; sort DESC server-side, no filters (Q5). Detail = per-order breakdown (order #, product price, platform fee, **net payout**) + fee-invoice download via the proven blob helper (`apiFetch parse:'blob'`, NOT the NSwag `Promise<void>` file gap) named `faktura-{batchNumber}.pdf`, shown iff `FeeInvoiceId != null`. **CSV never shown to makers** (Q4 — grep-for-absence AC). Read-only consumer of the T-0112/T-0112a contract — no backend change, no NSwag regen here. New `payouts-client.ts` + `dashboard.maker.payouts.*` i18n | 5 | M | **ready** | T-0103, T-0112, T-0112a | US-maker-0012, 0013 | 0022, 0024 |
| T-0117 | **Maker review-reply dashboard `/dashboard/maker/recenze`** (bundle `feat/reviews-loop-bundle`, maker frontend cap). New SSR `force-dynamic` route mirroring the `vyplaty` dashboard (T-0116): paged received-reviews card list (read-only `Stars`, customer first name + order number, comment / "bez komentáře", Czech date), aggregate header reading the maker's authoritative `RatingAverageBp`/`RatingCount` (no client averaging), URL-state `?page` pagination, empty/error states. One `'use client'` reply-form island: `Textarea` ≤500 chars pre-filled when editing → `respondToReview` POST → `router.refresh()`. New/extended `reviews-client.ts` (`getMakerReviews` + `respondToReview`), `dashboard.maker.reviews.*` i18n (tykání). **Locked:** Q4 reply OVERWRITABLE one-per-review ≤500 (no thread, no append), customer review immutable/read-only (no maker flag/hide/edit), Q5 live aggregate header from the maker field. Read-only consumer of T-0100 — no backend change, no NSwag regen here | 5 | S | **ready** | T-0100 | US-maker-0014 | 0013, 0022, 0024 |
| T-0118a | **Admin shell + read surfaces — dedicated `/admin/login`, overview KPIs, all-orders / all-invoices / audit-log lists** (L-split a of T-0118; **PR 1 alone**, the verification harness b/c assert their writes against). Read-only + auth gate: replaces the Phase-1 `(admin)/layout.tsx` skeleton with the real admin shell + server-side admin-session gate; **dedicated `/admin/login` posting `login('admin', …)` per ADR 0013** (NOT a reuse of the customer `/login`; middleware admin-branch retargeted `/login`→`/admin/login`); overview with deep-linking KPI tiles (counts from existing T-0111/0109/0106/0102 reads, missing counts render "—" + logged follow-up, NO backend here); three paged SSR URL-state-filtered lists (admin sees `customerEmail` — operator-only, no GDPR redaction). **Locked:** dedicated admin login (rejected customer-login reuse); KPIs from existing reads (rejected client-side count aggregation); type-to-confirm RESERVED for slice c (none here). Admin tone vykání; T8 gate live. Read-only consumer of the already-regenerated admin contract — **NO NSwag regen**. Invoice-download endpoint absent from `admin-api.v1.ts` → button ships disabled + logged follow-up, not wired to a guess | 5 | M | **ready** | T-0105, T-0108, T-0109, T-0111 | US-admin-0001, 0002, 0009, 0012, 0015 | 0005, 0012, 0013, 0014, 0022, 0024 |
| T-0118b | **Admin order detail + money/state action surfaces** `/dashboard/admin/orders/[orderId]` (L-split b of T-0118; **PR 2 pos 1**, bundled with c, risk-ascending b→c). Extends a's order list with the detail route (order header + audit trail) and the three write surfaces the order-cleanup bundle shipped endpoints for: **refund** (US-admin-0008, modal with money-grade disabled-button idempotency lock + `acknowledgePostPayout` checkbox when `Completed`), **manual state change** (US-admin-0010, modal with mandatory ≥10-char reason; allow-list backend-enforced, UI surfaces the named-command `order.manualTransition.*` codes), **dispute open/resolve** (US-admin-0011, inline structured form not a modal). **Locked:** refund=modal (rejected inline/optimistic/client-side cap); state=modal mandatory-reason backend allow-list (rejected client-side allow-list); dispute resolve=inline (rejected modal-for-resolve). All POST→`router.refresh()`, no optimistic UI; security_touching (money + state via admin commands). Read-only NSwag consumer — **no regen** (T-0105/0106/0107/0111 already regenerated). **Detail-DTO gap (Q-0024):** no admin order-detail read exists — header degrades to list-row + audit-log fields, thin `GetAdminOrderDetail` follow-up logged, NO backend added here | 5 | M | **ready** | T-0118a, T-0105, T-0106, T-0107 | US-admin-0008, 0010, 0011 | 0013, 0014, 0022, 0024 |
| T-0118c | **Admin ops + control-plane UI — outbox triage, country-config edit, payout view/complete/CSV, GDPR delete-user (the dangerous screen)** (L-split c of T-0118; **PR 2 pos 2**, lands last risk-ascending; the highest-risk frontend ticket — fronts the only irreversible hard-delete). Four control-plane surfaces over already-shipped commands: **outbox retry/ack** (T-0109 asymmetry — retry-on-processed hard 409, re-ack benign), **country-config edit** (T-0108 — provider change requires **retype-the-new-provider-code confirmation modal**, in-flight advisory non-blocking), **payout view + mark-completed + operator CSV** (T-0103 — VIEW+complete+CSV, NO manual create per A.3; **CSV operator-only, admin MAY download — inverts the T-0116 maker absence**, via `apiFetch parse:'blob'`), and the **delete-user GDPR screen** (T-0110 — **type-to-confirm the exact email** + proactive in-flight-order block + always-visible irreversibility banner; re-call → `user.notFound`="already deleted", no Silent-Success). **Locked:** type-to-confirm RESERVED exclusively for delete-user (rejected spreading to refunds); both delete gates surfaced pre-call (rejected post-submit 409); payout VIEW-only no create (rejected create-now button); admin CSV inverts maker absence; provider retype-the-code modal (rejected boolean checkbox). Admin tone vykání; T8 gate live; read-only consumer — **no regen**. **§secops Gate 3 + architect sign-off REQUIRED on the PR.** security_touching | 5 | M | **ready** | T-0118a, T-0102, T-0103, T-0108, T-0109, T-0110 | US-admin-0006, 0007, 0014, 0016 | 0004, 0013, 0014, 0017, 0020, 0022, 0024 |
| T-0126 | **Admin read follow-ups — invoice-PDF download + overview count endpoints** (closes **Q-0026** + **Q-0027**; unblocks T-0118a's degraded surfaces + serves T-0118c). Backend-only. **Q-0026:** `GET /api/v1/admin-invoices/{invoiceId}/pdf` on Web.Admin — controller-direct streaming per the T-0088 precedent with one swap: **Unscoped-by-invoice-id** (admin sees ANY invoice, no owner scoping) via a new read-only `IInvoiceRepository.GetByIdUnscopedReadOnlyAsync` (ADR 0025 mirror); 404 reuses `InvoiceNotYetRendered` (no new code); `private, no-store` + ETag/304 + `faktura-{InvoiceNumber}.pdf` (T-0064/T-0088 PII policy). **Q-0027:** two thin admin count endpoints (option a) — `GET /api/v1/payout-batches/count?state=Processing` → `{ count }` (new `IPayoutBatchRepository.CountByStateAsync`) + `GET /api/v1/outbox-events/stalled/count` → `{ count }` (new `IOutboxConsumerRepository.CountStalledAsync`, predicate `ProcessedAt==null AND NextRetryAt==null AND LastErrorKind!=None` per T-0109's stalled set). Globally-unique Response names; NO new error codes / migrations / outbox / i18n. `[Authorize]` admin audience on all 3; one NSwag regen (admin host — all 3 new on it). **T-0118a FE re-enable rides a tiny FE follow-up — NOT in scope.** security_touching (admin file streaming + Unscoped reads) | 5 | S | **ready** | T-0088, T-0111, T-0102, T-0109 | US-admin-0002, 0012 | 0013, 0022, 0023, 0025 |
| T-0127 | **Admin read gaps — country-config GET + admin order-detail + stalled-outbox/payout-batch LIST + 3 frontend re-wires** (closes **Q-0029** (all four admin reads) + **Q-0024** (admin order-detail); **removes the PR-2 country-config full-replace fence**; completes the admin read-side). Cross-stack, one PR. Four thin admin-host reads mirroring the T-0111 `IAdminQueries` precedent (AsNoTracking, `Unscoped()`, paged-where-listy, globally-unique Response, `[Authorize]` admin): **(1 PRIORITY)** `GET /api/v1/country-configurations/{code}` → the **exact** `UpdateCountryConfiguration` Response field set so the T-0118c form PRE-FILLS (404 reuses `CountryConfigurationNotFound`, no new code); **(2)** `GET /api/v1/admin-orders/{orderId}` → privileged `AdminOrderDetailDto` over `GetByIdUnscopedAsync` (no GDPR redaction; 404 reuses `OrderNotFound`) + a `customerUserId`/`makerId` filter on the admin-orders read = the per-user in-flight signal for the delete-user proactive pre-disable; **(3)** `GET /api/v1/outbox-events/stalled` (paged) reusing the **exact** T-0126/T-0109 stalled predicate `ProcessedAt==null && NextRetryAt==null && LastErrorKind!=None`; **(4)** `GET /api/v1/payout-batches` (paged, Unscoped — GET on the existing CreatePayoutBatch POST route). **FE re-wires:** country-config pre-fill + diff-based provider modal (T-0118c AC-4/AC-5 now met) + banner downgrade; order-detail header onto the real DTO; delete-user proactive in-flight pre-disable (T-0118c AC-12); browsable paged outbox + payout lists w/ URL-state pagination. **Locked:** country-config GET pre-fill + diff-modal; per-user in-flight = orders filter (over a boolean); stalled predicate reuse; payout LIST = GET on existing route. NO new codes / migrations / outbox / i18n / unique index. NSwag regen admin host (4 new methods); `npm run check:api` re-enabled. security_touching (admin Unscoped reads + delete-user per-user signal) | 5 | M | **ready** | T-0108, T-0111, T-0118b, T-0118c, T-0126 | US-admin-0006, 0007, 0009, 0014, 0016 | 0013, 0022, 0023, 0025 |
| T-0119 | Frontend: /dashboard/admin/categories CRUD + /dashboard/admin/makery list (verify, deactivate, refresh-ARES) | 5 | M | draft | T-0040, T-0034 | US-admin-0003, 0004, 0005, 0013 | — |

**Phase 5 total:** 27 tickets (T-0127 added at the 2026-06-15 admin-read-gaps grooming — the cross-stack bundle closing Q-0029 (all four admin reads) + Q-0024 (admin order-detail), listed next to T-0126 it shares the admin-read shape + stalled predicate with; removes the PR-2 country-config full-replace fence + completes the admin read-side; T-0126 added at the 2026-06-15 admin-reads-followups grooming — the backend follow-ups closing Q-0026 (admin invoice-PDF download) + Q-0027 (overview count reads) logged by T-0118a, listed here next to the T-0118 slices it unblocks; T-0102 split a/b at the 2026-06-12 payout-core-bundle grooming; T-0112a added at the 2026-06-13 payout-settlement-bundle grooming — the T-0088 Customer-only fee-invoice-download gap; T-0050 added at the 2026-06-14 reviews-loop-bundle grooming — the deferred public review-list slice, listed here next to its blocker T-0100 rather than in its numeric Phase-3 catalog range per the split/deferred-ticket placement convention; T-0108–T-0111 groomed `draft → ready` at the 2026-06-14 admin-ops-bundle deliberation — four-ticket admin-control bundle, one PR, implemented risk-ascending T-0111 → T-0109 → T-0108 → T-0110 though numbered T-0108..T-0111 here; T-0118 L-split into T-0118a/b/c at the 2026-06-15 admin-dashboard-grooming deliberation — `+2` net, shipping in **two PRs**: a (shell + dedicated `/admin/login` + read lists + overview KPIs — the verification harness) alone in PR 1, then b (order refund/state/dispute actions) + c (ops + control-plane + the GDPR delete-user dangerous screen) bundled risk-ascending b → c in PR 2. **Dependency-gap fix:** T-0118's row listed `T-0102` but omitted **T-0110** (GDPR erase) + **T-0103** (payout complete + BankReference) — both consumed by T-0118c and now in its `depends_on`; Q-0025 records the fix).

---

## Cross-cutting follow-ups (queued during sprint reviews)

| Ticket | Title | Phase | Size | State | Depends on | Stories | ADRs |
|---|---|---|---|---|---|---|---|
| T-0123 | Dedicated migration-pipeline assertions on top of the Postgres `PostgresHarness` shipped by T-0062: explicit tests that walk the migration journal, assert every expected schema object exists, and pin column types / indexes / partial-index filters. (T-0062 ships the Testcontainers harness itself + calls `MigrateAsync` to set up; T-0123 adds the assertion layer to fully close the gap surfaced by T-0060 third Copilot review where SQLite `TestDbHarness` uses `EnsureCreated()`.) | 4 | S | draft | T-0002, T-0062 | — | — |
| T-0124 | Migrate existing direct-DI provider adapters (`SendGridEmailProvider` for `IEmailProvider`, `AresCompanyRegistry` for `ICompanyRegistry`) to the keyed-services-with-factory pattern T-0065 introduces. Adds `IEmailProviderFactory` + `ICompanyRegistryFactory` keyed by `CountryConfiguration.DefaultEmailProvider` + `DefaultRegistry`. Closes the lone-keyed-provider awkwardness left by T-0065 and aligns with ADR 0008 "provider factory" mandate. | 4 | M | draft | T-0065 | — | 0008 |
| T-0125 | **Debt-codification bundle** (chore, `chore/debt-codification-bundle`). Codifies recurring-findings **#2** (T8 i18n-parity check — every `BusinessErrorMessage` code is a `cs-CZ` key OR in the `T8_NO_KEY_REQUIRED` allowlist; 70 seeds; `hard`, not baselined) + **#3** (T9 unique-index→translator check — every NAMED `.IsUnique().HasDatabaseName()` index is a `UniqueConstraintTranslator` key OR carries a `// no-translator:` marker; `hard`; 5 markers added) into `scripts/check-consistency.mjs`. Fixes the latent `ux_disputes_order_open` translator bug (maps the concurrent-double-`OpenDispute` race-loser to `OrderInvalidTransition` 409, not a 500; + race integration test). Rides along **Q-0013** (`/auth/login`→`/login` 8-ref frontend sweep) + **Q-0019** (`ix_orders_payout_unclaimed` partial index + migration). **Q-0017** verify-only (already shipped). `check-consistency.mjs` exits 0 on master after the bundle; closes Q-0013 + Q-0019 on merge. | 4 | M | **ready** | — | — | 0009, 0013, 0022, 0023 |

---

## Phase 6 — Polish (static pages, content, SEO, manual ops checks)

| Ticket | Title | Phase | Size | State | Depends on | Stories | ADRs |
|---|---|---|---|---|---|---|---|
| T-0130 | **Static public pages — content half of `feat/public-polish-bundle`** (user-locked 2026-06-20; ships in one PR with T-0131). Four new `(public)` Server-Component pages. **/jak-to-funguje + /pro-makery ship REAL marketing content** harvested from PROJEKT-VIZE.md (§"Jak to funguje — objednávkový flow" customer step cards; §"Byznys model"/§"6 kategorií" maker value-prop: registrace zdarma/IČO, provize 15 %, týdenní výplaty, automatická fakturace, doprava Zásilkovna, žádné minimum) — every claim traceable to a vize section, rebranded Tiskni.cz→Makables; **vykání** (public acquisition surface — the tykání-for-makers decision governs the authenticated dashboard, not this page). **/vop + /gdpr ship PLACEHOLDER shells, NOT invented legal text** (legal-placeholder lock): real route + nav + wired-empty `static.terms.*`/`static.privacy.*` i18n keys + a VISIBLE `Alert variant="warning"` "PLACEHOLDER — awaiting approved legal text (JVM YORE s.r.o.)" — the agent does NOT draft terms/privacy/cookie copy (legal liability). The legal TEXT is a **blocking pre-launch item: Q-0030** + a `docs/launch-checklist.md` line; T-0130 merges without it. New `static.*` cs-CZ namespace; `generateMetadata` title/description baseline (T-0131 extends OG/canonical). **No backend touch, no NSwag regen, no error codes, no migration.** | 6 | M | **ready** | T-0015 | — | 0022 |
| T-0131 | **SEO surface — SEO half of `feat/public-polish-bundle`** (user-locked 2026-06-20; same PR as T-0130, lands after it). Next.js `MetadataRoute` `sitemap.ts` + `robots.ts` (conventions, NOT hand-rolled XML) + OpenGraph/Twitter-summary/canonical on the four indexable page types (landing `/`, `/katalog`, `/katalog/[slug]` `type=profile`, `/produkt/[productId]` `type=product`). **SEO scope (locked):** robots **allow-all at MVP, no `Disallow`** (dashboards are auth-gated, not robots-hidden — a `Disallow` would advertise `/admin`); sitemap always enumerates the six static routes (`/`, `/katalog`, `/jak-to-funguje`, `/pro-makery`, `/vop`, `/gdpr`), **enumerates maker slugs** via the existing `getPagedMakers` paged walk (capped), **defers product-id enumeration** (no bulk read — flagged `// TODO`), and **falls back to static-only without 500** on a catalog-read failure; **text-only `summary` card** (no OG image asset at MVP — launch-checklist follow-up); canonical **excludes filter query params** (duplicate-content hygiene); **no JSON-LD, no hreflang** (cs-CZ only) at MVP. Introduces the missing absolute-URL base: new non-secret **`NEXT_PUBLIC_SITE_URL`** (default `https://makables.cz`) read via `lib/seo/site-url.ts` + `metadataBase` on the root layout. Adds `generateMetadata` to the landing page (it had none) → 2 new `home.metadata.*` keys; all other OG strings reuse existing meta. SEO tests (sitemap/robots/site-url/landing-metadata). **No backend change, no NSwag regen, no new error codes, no migration.** Manual step: set `NEXT_PUBLIC_SITE_URL` in prod/staging. | 6 | M | **ready** | T-0046, T-0047, T-0048, T-0130 | — | 0022, 0023 |
| T-0132 | **k6 load test — k6 half of `feat/quality-gates-bundle`** (Bundle B, user-locked 2026-06-21; ships in one PR with T-0133). **codebase-now + staging-gated RUN.** k6 JS script in `deploy/load-tests/` (ADR 0023 §6 path): 100 concurrent VUs / 30-min / mixed catalog-browse (GET /katalog, /produkt) + order-placement, configurable `BASE_URL`. **ADR 0023 §1 budgets baked as k6 thresholds:** catalog SSR p95 400ms/p99 1000ms, product SSR p95 350ms, order API p95 600ms/p99 1500ms; `http_req_failed rate==0` (zero 5xx). **PASS = thresholds met + zero 5xx + DB CPU <70% (out-of-band — k6 cannot read Azure Postgres CPU).** README in the load-tests dir: install/seed/run/read + staging-only/never-production. **The 30-min RUN is a `manual_step`** (live seeded staging + human; pre-launch, NOT a merge gate). **Q-0015 fold: bundle-budget N/A — k6 is API latency, not JS bundle; Q-0015 stays a separate deferred FE-perf concern.** No app code, no NSwag, no migration, no error codes, no i18n. (INDEX `depends_on` "All Phase 1-5" = the full catalog+order path must be live for a meaningful run; the ticket pins T-0046/0048/0063/0065/0085.) | 6 | S | **ready** | All Phase 1-5 | — | 0023 |
| T-0133 | **Accessibility — a11y half of `feat/quality-gates-bundle`** (Bundle B, user-locked 2026-06-21; same PR as T-0132). **Q-0031 ANSWERED: stand up vitest + @testing-library/react + jest-axe as THE frontend harness** (real harness, not a standalone axe script) — devDeps in `frontend/package.json` + lockfile + `vitest.config.ts` (excludes the generated `api-client/`) + a `"test"` script. **axe-core a11y tests** asserting **zero WCAG 2.1 AA violations** (ADR 0023 §5) on the critical customer paths (catalog, product, order/checkout surfaces, the four static pages); small noted fixes for any violation (contrast/label/focus/alt) — **i18n keys preserved (T8/T9 green)**. **axe-in-CI:** add a frontend `npm run test` step to `.github/workflows/ci.yml` so a11y regressions fail CI. **SEO predicate tests (Q-0031 retroactive unblock of T-0131):** pin `canonicalUrl` + the pure SEO/format predicates the SEO ticket left untested. **Manual a11y checklist** `docs/test-plans/a11y-manual-checklist.md` — keyboard-nav + **NVDA/Firefox Czech screen-reader** pass (ADR 0023 §5); **the RUN is a `manual_step`** (human + screen reader; pre-launch). No backend change, no NSwag regen, no migration. | 6 | M | **ready** | T-0046, T-0047, T-0048, T-0084a, T-0084b, T-0085, T-0086a, T-0086b, T-0130, T-0131 | — | 0023 |
| T-0134 | Production secret rotation playbook + monitoring playbook + restore-from-backup playbook (`docs/runbooks/`) | 6 | M | **ready** | T-0016, T-0014 | — | 0023 |
| T-0135 | Bug bash + final manual smoke against staging | 6 | M | draft | All other tickets | — | — |
| T-0136 | **Rate-limit mount — half of `feat/secops-hardening-bundle`** (user-locked 2026-06-21; ships in one PR with T-0137). **Closes Q-0011.** The per-audience "default" fixed-window policy (cust 100 / maker 60 / admin 30 / public 60 per min) is defined in `AddMakablesRateLimiting` but mounted NOWHERE (no `GlobalLimiter`, no `[EnableRateLimiting("default")]`) — every endpoint except the two partitioned ones (`addresses-autocomplete`, `shipping-widget-config`) is unlimited, including the 14 anonymous auth endpoints + `PostMessage` (2000-char bodies). **Mount "default" as the per-host `GlobalLimiter`** (partition: per-`sub` authenticated / per-IP anonymous, `ip:unknown` fallback — mirrors `PartitionAutocomplete`) + **add a tight per-IP `"auth"` policy (10/min, no queue)** class-level on `AuthController` (the brute-force/stuffing surface; ADR 0023 §4 already alerts "failed login >50/min/IP"). `OnRejected` emits `Retry-After`; status stays 429. **429 is a raw middleware rejection — NO `BusinessErrorMessage`, NO cs-CZ key, NO NSwag regen, NO migration.** In-memory per-instance (single-region MVP; distributed store flagged v1.1). Integration tests (global 429, auth 429, no-regression on the two existing policies, `Retry-After`) + partition-key unit test. | 6 | S | **ready** | T-0008, T-0035 | — | 0023 |
| T-0137 | **Admin PII-read audit — half of `feat/secops-hardening-bundle`** (user-locked 2026-06-21; same PR as T-0136). **Closes Q-0028.** Admin auditing is command-only (`AdminAuditPipelineBehavior` + UoW commit); privileged admin READS of customer PII leave no trail: invoice-PDF download (`GET /admin-invoices/{id}/pdf`, recipient PII), payout CSV download (`GET /payout-batches/{id}/csv`), and order detail (`GET /admin-orders/{orderId}`, full contact snapshot, no GDPR redaction). **New `IAdminReadAuditWriter`** (Core.Domain) + impl owning its OWN `MakablesDbContext` via the registered `IDbContextFactory` (T-0032 ARES-cache precedent — commits OUTSIDE the request UoW so a pure read never opens a write txn / calls `SaveChangesAsync` in a handler; T3 stays green). Audits **only the successful PII-read path** (`invoice.pdf.download` / `payout.csv.download` / `order.detail.view`; NOT 404/304/409). `beforeJson=afterJson=null` (reads have no delta). **REUSES `admin_audit_log` (T-0011) — NO migration, NO new error code, NO i18n, NO NSwag.** Skips the high-volume LIST reads (low forensic value). Integration tests (each 200 writes one row; 404/304/409 write zero) + writer unit test. | 6 | M | **ready** | T-0011, T-0126, T-0127 | US-admin-0012 | 0014 |

**Phase 6 total:** 8 tickets (T-0134 groomed `draft → ready` at the 2026-06-21 ops-runbooks grooming — lean doc-only secops bundle on `feat/ops-runbooks`: three runbooks in a new `docs/runbooks/` dir (secret-rotation + monitoring + backup-restore) grounded in the shipped T-0016 Bicep + ADR 0023 §4/§7; `depends_on` corrected to add T-0014 observability; `security_touching: yes`; per-runbook staging dry-run is the SecOps pre-launch manual_step. **T-0136 + T-0137 groomed `ready` at the 2026-06-21 secops-hardening grooming — the last two pre-launch security gaps: Q-0011 rate-limit mount + Q-0028 admin PII-read audit; both `security_touching: yes`, both code-only, both ship in `feat/secops-hardening-bundle`.**).

---

## Totals

| Phase | Tickets | Effort (approx) |
|---|---|---|
| 1 — Foundation scaffold | 16 | ~10 days |
| 2 — Identity | 17 | ~12 days |
| 3 — Catalog | 14 | ~6 days |
| 4 — Orders | 28 | ~20 days |
| 5 — Post-order | 27 | ~15 days |
| 6 — Polish | 8 | ~6 days |
| **Total** | **103** | **~66 days** of agent-equivalent work |

This is a rough rolling estimate. Some tickets in Phase 4 will reveal sub-tickets during implementation (e.g. label-streaming edge cases). PM splits as needed.

---

## Sprint plan (proposed)

The agents work continuously, but it's helpful to define checkpoints. Sprint = ~1 work week, ~10–15 tickets done.

| Sprint | Phases covered | Goal at end |
|---|---|---|
| 1 | Phase 1 (1–16) | Solution scaffolded, hosts run, OpenAPI emitted, NSwag pipeline works, Bicep deploys an empty environment |
| 2 | Phase 2 first half (20–32) | Auth (password + magic link + Google OAuth + reset + confirmation) end-to-end; addresses; geocoding; ARES lookup |
| 3 | Phase 2 second half (33–36) + Phase 3 first half (40–42) | Maker registration end-to-end; product CRUD with images |
| 4 | Phase 3 second half (43–49) | Catalog, maker profile, product detail pages live |
| 5 | Phase 4 first third (60–69) | Order placement → payment → invoice generation works end-to-end (no UI for tracking yet) |
| 6 | Phase 4 second third (70–79) | Shipping (Zásilkovna + personal pickup), accept/ship/deliver, messages |
| 7 | Phase 4 last third + Phase 5 first quarter (80–87, 100, 101) | Order UI complete; reviews entity in place; payout entity scaffolded |
| 8 | Phase 5 second half (102–112) | Weekly payouts, refunds, disputes, admin operations |
| 9 | Phase 5 last + Phase 6 (113–135) | Frontend admin UI; polish; load test; accessibility; runbooks |
| 10 | Production launch sprint | Deploy to production, soft launch, watch metrics |

Total: ~10 working weeks from sign-off to production launch. Conservative.

---

## Status

All tickets are in `draft`. PM expands each to a full ticket file on transition to `ready`. Builds begin in Phase 0.6 (scaffold) after user sign-off (Batch 10).
