namespace Makables.Core.AppServices;

/// <summary>
/// Marker type for assembly scanning (MediatR handlers, FluentValidation validators) via
/// <c>typeof(AssemblyReference).Assembly</c>. Intentionally empty.
/// </summary>
public static class AssemblyReference
{
}
