namespace Makables.Core.Domain;

/// <summary>
/// Marker type for assembly scanning (e.g. EF Core <c>IEntityTypeConfiguration</c> discovery
/// via <c>typeof(AssemblyReference).Assembly</c>). Intentionally empty.
/// </summary>
public static class AssemblyReference
{
}
